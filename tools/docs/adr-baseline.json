{
 "0001-validate-dealership-opening-hours.md": {
  "options": [
   "--",
   "A technician can still be booked outside their personal working hours; a dealership open 0",
   "A-8's instants-at-the-boundary reading is vindicated rather than complicated — the zone li",
   "Bad, because a scheduler that confirms 03:00 on a Sunday is not credible as a replacement ",
   "Bad, because it introduces the only wall-clock/zone reasoning in the system, and zone bugs",
   "Bad, because it is a partial answer, and a partial answer invites the reasonable objection",
   "Bad, because it would be the first place in the design where the exclusion constraint stop",
   "Bad, because shift and absence data is per-resource and time-varying, so it lands in the c",
   "Bad, because shift patterns, rotations, holiday entitlement and absence are a scheduling d",
   "Bad, because the argument for it (\"a second rule is dangerous\") over-generalises: it treat",
   "Bad, or deferred",
   "Closures, public holidays and one-off exceptions are not modelled. A dealership is open on",
   "Dealership opening hours are a static property of the request. Given the dealership id, th",
   "Good",
   "Good, because \"available\" would mean what a service manager means by it, and the system co",
   "Good, because it buys the visible half of the credibility for a fraction of the model: two",
   "Good, because it is decidable from the request alone, so it cannot reintroduce check-then-",
   "Good, because it is the smallest possible answer and leaves exactly one notion of validity",
   "Good, because it removes the timezone question entirely — no wall-clock comparison exists ",
   "Good, because the failure mode is a plain 400 on a path that never touches contention, kee",
   "Holidays and one-off closures are absent, so a dealership shut on 25 December will accept ",
   "Opening hours are per dealership and per day of week, held as seeded reference data (A-6, ",
   "Option A — Unbounded time. No working-window rule of any kind. Validity is non-overlap and",
   "Option B — Opening-hours validation only. Each dealership carries opening hours as referen",
   "Option C — Full working-calendar modelling. Dealership opening hours plus technician shift",
   "Quality goal 1 is untouched. There is exactly one class of availability rule and the datab",
   "Reference data gains two fields (opening hours, IANA zone) that A-6's seed and A-7's \"no r",
   "Technician shifts, holidays and absence are properties of a resource over time. They are",
   "The dealership carries an IANA time zone. Comparison is: convert the request's instant int",
   "The rejection is cheap and early: it needs no candidate query, so an out-of-hours request ",
   "The system no longer books at 03:00, and the rule that stops it is a pure function that a ",
   "The whole derived interval must fall within opening hours — both the start and the end der",
   "Wall-clock comparison against a zone is the one place in the system where a DST transition"
  ],
  "decision": "Chosen option: Option B — opening-hours validation only, because it removes the visibly absurd"
 },
 "0002-service-advisor-actor-no-authentication.md": {
  "options": [
   "(The architect's recommendation.)",
   "--",
   "Authentication, authorisation, sessions, rate limiting and per-actor audit are out of scop",
   "Bad, because \"ownership is validation\" is a decision that quietly hardens: the longer test",
   "Bad, because it adds friction to the cURL harness while providing no property any test cou",
   "Bad, because it authenticates nobody in particular: with a single shared credential, owner",
   "Bad, because it requires an identity provider, token issuance and a session story for a cl",
   "Bad, because the interesting risk in this system is concurrent resource allocation, and ev",
   "Bad, because the resulting service could not be deployed anywhere reachable, and says so.",
   "Bad, or deferred",
   "Customer self-service. The caller is the vehicle owner. customerid must not be in the body",
   "Every 4xx on the booking path is about the domain — unknown reference, mismatched ownershi",
   "Good",
   "Good, because it is the only reading under which the service is safe on its own, and the o",
   "Good, because it keeps customerid an ordinary field, so the booking request stays a single",
   "Good, because it matches the brief's framing — replacing a manual booking process run by s",
   "Good, because it spends no review budget on a mechanism the assessment does not grade.",
   "Good, because it would force an explicit answer to \"what may this caller see?\", which a re",
   "Good, because the contract would show a security scheme, which looks more production-shape",
   "No security mechanism is claimed that is not tested. Nothing in the submission asserts a p",
   "Option A — Service advisor, no authentication. The caller is trusted. customerid travels i",
   "Option B — Customer self-service with authenticated identity. Identity arrives with the re",
   "Option C — Service advisor, with a shared API key or token on the boundary. Authentication",
   "Ownership is a validation rule, not a security control. \"The vehicle must belong to the na",
   "Rate limiting is absent, so the retry loop of ADR-0004 has no per-caller ceiling above its",
   "Retrofitting authentication is not purely additive: the ownership check moves layer, its s",
   "Service advisor on behalf of a customer. The caller is dealership staff, typically on the ",
   "The API is fully exercisable by cURL with no credential ceremony, which is what makes the ",
   "The caller is trusted. The stubbed client layer stands in for an authenticated front end t",
   "The customer is not an actor, which keeps §3.1's boundary to two human roles and one store",
   "The service is unsafe to expose. Anyone who can reach it can book on any customer's behalf",
   "There is no actor on an appointment record — no \"booked by\" — so the audit question (\"who ",
   "customerid travels in the request body, alongside vehicle, service type, dealership and"
  ],
  "decision": "Chosen option: Option A — service advisor, no authentication, because the actor the brief's"
 },
 "0003-cancellation-and-rescheduling-in-scope.md": {
  "options": [
   "(The architect's recommendation.)",
   "--",
   "A move must never be a delete-then-insert, or a cancel-then-insert. This is the explicit",
   "A move onto an interval that overlaps the appointment's own current interval must succeed ",
   "Bad, because \"deferred to a backlog slice with a proposed ADR\" would have parked the singl",
   "Bad, because an appointment that can never be cancelled leaves a slot occupied forever, wh",
   "Bad, because it changes the appointment's id on every move, breaking every reference a cal",
   "Bad, because it expands scope past the recommendation and costs at least one more slice.",
   "Bad, because it leaves out the case where the design is most likely to be got wrong, and t",
   "Bad, because it relies on the reader understanding that the row does not conflict with its",
   "Bad, because it transiently releases the slot. In the two-transaction form the release is ",
   "Bad, because it turns a free failure (\"refused, original stands\") into a compensation prob",
   "Bad, because the constraint mandated by CLAUDE.md §2.1 already presupposes cancellation, s",
   "Bad, or deferred",
   "Cancellation has no policy attached — no notice period, no fee, no restriction on cancelli",
   "Good",
   "Good, because it introduces no second mechanism: one constraint covers booking, moving, an",
   "Good, because it is the minimum that makes the partial constraint meaningful and testable.",
   "Good, because it is the narrowest reading of the three stated criteria, and the smallest s",
   "Good, because it preserves the appointment's identity across a move, which is what a calle",
   "Good, because it protects the WIP limit and the scarce human review budget (OC-3).",
   "Good, because it reuses the booking path verbatim and needs no new write shape, which is w",
   "Good, because the property that matters — a refused move leaves the original intact — fall",
   "No AND id <> :id predicate, no pre-read of the target slot, and no application-side \"is it",
   "Only a confirmed appointment may be moved; rescheduling a cancelled one is refused. The ex",
   "Option A — Neither in scope. Booking only. The WHERE (status <> 'cancelled') clause stays ",
   "Option B — Cancellation only; rescheduling deferred to a backlog slice.",
   "Option C — Cancellation and rescheduling, the move being a single atomic UPDATE on the exi",
   "Option D — Cancellation and rescheduling, the move being implemented as a compensating pai",
   "Scope grows beyond the architect's recommendation: two more endpoints, an appointment stat",
   "The appointment's identity is preserved: the id a caller was given at booking still names ",
   "The concurrency story gets its second act with no second mechanism: reschedule, book, and",
   "The exclusion constraints on bayid and technicianid are checked against the new row versio",
   "The lifecycle is complete enough to be credible — a booking system that cannot change a bo",
   "The most plausible race a competent implementer could still introduce is named and prohibi",
   "The new interval is validated against the dealership's opening hours (ADR-0001) before any",
   "The partial predicate WHERE (status <> 'cancelled') acquires behaviour, and therefore a te",
   "The write path is no longer a single insert. A-6's rationale — that keeping booking a sing",
   "There is no history: a moved appointment shows only its current interval. Who moved it and",
   "Two racing reschedules onto the same slot behave exactly like two racing bookings: one com",
   "Where the move needs a different bay or technician, candidate selection and the retry poli"
  ],
  "decision": "Chosen option: Option C — both in scope; a move is one atomic UPDATE."
 },
 "0004-retry-across-remaining-candidates.md": {
  "options": [
   "(The architect's recommendation.)",
   "--",
   "A 409 comes to mean the dealership had nothing free, which is what the caller will read it",
   "Bad, because it caps a dealership's booking throughput at one at a time, converting the de",
   "Bad, because it has no natural bound: under sustained contention the read can keep returni",
   "Bad, because it is more code on the most safety-critical path in the system, and the per-a",
   "Bad, because it is the variant that most resembles check-then-act — a read-write-read-writ",
   "Bad, because it makes 409 ambiguous between \"no capacity\" and \"unlucky selection\", degradi",
   "Bad, because it moves correctness back into application code and lock discipline. The guar",
   "Bad, because it multiplies database round trips on the contended path for a benefit that i",
   "Bad, because it needs a second quality scenario (no spurious refusal) that is harder to wr",
   "Bad, because it refuses bookings the dealership could have honoured, which the service man",
   "Bad, because it would be a standing invariant overturned by a policy decision, which is no",
   "Bad, or deferred",
   "Candidate ordering is an architecture decision for Gate B, not a Gate A ruling. It is call",
   "Capacity is actually used under burst load — the 08:00-on-Saturday case in §1.1 that motiv",
   "Each attempt is independently recoverable. A constraint violation aborts the enclosing",
   "Every conflict is observable. bookingconflictstotal{resource} counts each 23P01, and an",
   "Good",
   "Good, and honestly so: under a per-dealership lock (or true SERIALIZABLE) a check-then-act",
   "Good, because each attempt uses the freshest information, so it would in principle waste f",
   "Good, because it eliminates spurious refusals completely and deterministically, with no re",
   "Good, because it removes the spurious refusal while leaving the correctness mechanism unto",
   "Good, because it terminates by construction — the candidate set is finite, read once, and ",
   "Good, because no bound, no savepoint discipline and no cap are needed — three ways to get ",
   "Good, because the booking path stays a single attempt: read candidates, insert, map 23P01 ",
   "Good, because the resulting 409 is honest, and the conflict metric measures contention rat",
   "Latency under contention is worse: a losing request may perform several round trips before",
   "No overlap. Under any interleaving of concurrent requests, no two non-cancelled appointmen",
   "No spurious refusal. Given N concurrent requests for one interval at a dealership with M",
   "Only 23P01 retries. Any other SQLSTATE surfaces as itself — a foreign-key violation means ",
   "Option A — Refuse immediately. The first 23P01 becomes a 409. One attempt per request.",
   "Option B — Retry across the remaining candidates from the single advisory read, then refus",
   "Option C — Retry with a fresh candidate read after each failure. On 23P01, re-query",
   "Option D — Serialise bookings per dealership. Take a per-dealership advisory lock (or run ",
   "Rescheduling uses the same policy, with the UPDATE of ADR-0003 as the attempted write.",
   "Retry work grows with the square of concurrency in the worst case if candidate ordering is",
   "The booking path is materially more complex than a single insert: a loop, per-attempt tran",
   "The cap introduces a rare outcome — refused while candidates remained — that is a spurious",
   "The invariant stays exactly where CLAUDE.md §2.1 put it. This decision adds a policy above",
   "Validation is not repeated. Opening hours (ADR-0001) and reference integrity are propertie",
   "bookingconflictstotal becomes a real signal: a conflict now records genuine contention rat"
  ],
  "decision": "Chosen option: Option B — on 23P01, attempt the next candidate; refuse only when the candidate"
 },
 "0005-fastify-with-typebox-schemas.md": {
  "options": [
   "--",
   "400 versus 422 is a structural distinction — schema layer versus domain layer — not a",
   "@fastify/swagger's output is only as good as the schemas; a route with a loose response sc",
   "Bad, and decisively: the OpenAPI document would be hand-authored, which moves the contract",
   "Bad, because Fastify's JSON-Schema dialect support lags the spec, so an exotic schema cons",
   "Bad, because Hono's centre of gravity is edge and Workers runtimes; on Node its pg, pino a",
   "Bad, because TypeBox's error messages are poor and its generic types are hard to read.",
   "Bad, because Zod schemas must be converted to JSON Schema to become OpenAPI, so the contra",
   "Bad, because hand-rolled infrastructure is the thing a reviewer must read most carefully a",
   "Bad, because its module system is a second answer to \"what is a layer\" alongside",
   "Bad, because its swagger output is driven by decorators that are separate from the validat",
   "Bad, because response serialisation is untyped, so a handler can return a field the contra",
   "Bad, because routing, body parsing, schema validation, serialisation, error rendering and ",
   "Bad, because the decorator-and-DI ceremony is a large fraction of the codebase for four en",
   "Bad, or deferred",
   "Every route declares schema: { body, params, querystring, response } as TypeBox types.",
   "Fastify ships pino as its logger, so §1.2 goal 4's log correlation is the default rather t",
   "Fastify's hook model gives one place to attach the request span and the pino child logger ",
   "Fastify's plugin encapsulation is a second scoping mechanism beside the module layering of",
   "Good",
   "Good, because @hono/zod-openapi genuinely generates the document from the schemas, so it s",
   "Good, because Express 5 is stable and its middleware model is trivially inspectable.",
   "Good, because Zod's error messages are far better than TypeBox's.",
   "Good, because it has no dependencies and nothing is hidden.",
   "Good, because it imposes a module structure and DI out of the box, and @nestjs/swagger pro",
   "Good, because it is a thin edge: routes, hooks, an error handler, and nothing that claims ",
   "Good, because it is the most widely understood option, so a reader needs no explanation.",
   "Good, because one schema declaration serves validation, typing, serialisation and the cont",
   "Good, because pino and OpenTelemetry integration are first-class and already required by T",
   "No security scheme is published, per ADR-0002 / GC-2. The absence is a statement in the co",
   "Option A — Fastify, with TypeBox schemas on every route and @fastify/swagger emitting the",
   "Option B — Express (5.x) with a validation middleware (zod/ajv) and a separately authored",
   "Option C — Hono with @hono/zod-openapi.",
   "Option D — NestJS with @nestjs/swagger decorators.",
   "Option E — node:http directly, hand-rolled routing and validation.",
   "Schema failure is the 400 of §8.6, produced by Fastify's error hook before any handler run",
   "The HTTP layer may not reach the database: .dependency-cruiser.js forbids src/http → src/p",
   "The OpenAPI document cannot drift from the implementation, because it is not written.",
   "The OpenAPI document is emitted at build time from the running route table into",
   "TypeBox is a second thing to learn beside Fastify, and its type-level machinery produces",
   "generated tier, and only a schema-first framework can put it there."
  ],
  "decision": "Chosen option: Option A — Fastify with TypeBox route schemas."
 },
 "0006-kysely-as-typed-sql-builder.md": {
  "options": [
   "--",
   "23503 (foreign key) stays distinguishable from 23P01 (exclusion), which ADR-0004 requires ",
   "A column renamed by a migration for A-1 or A-4 fails the build rather than one test, which",
   "ADR-0003 — a reschedule is one atomic UPDATE on the existing row, with no AND id <> :id",
   "ADR-0004 — on 23P01 the next candidate is attempted, and each attempt must be independentl",
   "Bad, and disqualifying: exclusion constraints and tstzrange do not exist in the Prisma sch",
   "Bad, and independently disqualifying: it wraps errors. A constraint violation from $execut",
   "Bad, because an ORM's natural idiom for a reschedule is load, mutate, save, which is a rea",
   "Bad, because both wrap driver errors (QueryFailedError, DatabaseError) with inconsistent",
   "Bad, because entity mapping and change tracking put an object graph between the code and t",
   "Bad, because it is one more dependency than Option A for a benefit that only pays off when",
   "Bad, because range types require a custom type definition, so the tstzrange handling is",
   "Bad, because row and parameter types are hand-maintained assertions. A migration that rena",
   "Bad, because that folding is the problem: drizzle-kit generate derives migrations from the",
   "Bad, because the Database interface can drift from the migrations until the regeneration c",
   "Bad, because the availability query (multi-table, dealership-scoped, qualification-joined,",
   "Bad, because the retry loop's per-attempt statement scoping fights Prisma's interactive-tr",
   "Bad, or deferred",
   "Good",
   "Good, because both are mature, familiar, and come with migrations and a repository pattern",
   "Good, because drizzle-kit would fold ADR-0007 into this decision, which is fewer moving pa",
   "Good, because it has the best developer experience in the field and generates its own type",
   "Good, because it is a builder rather than a mapper, so the SQL it emits is predictable fro",
   "Good, because it is maximally transparent: every statement is exactly what is sent, and th",
   "Good, because it is one dependency, and one the other options all depend on anyway.",
   "Good, because it is the option that most obviously cannot hide the invariant.",
   "Good, because it satisfies the disqualifying test outright: it does not wrap driver errors",
   "Good, because it types the schema without owning it — migrations stay plain SQL (ADR-0007)",
   "Good, because like Kysely it compiles to SQL, keeps the driver error intact, and is fully ",
   "Good, because sql fragments make the PostgreSQL-specific parts explicit rather than smuggl",
   "Kysely is less widely known than Prisma or TypeORM, so a reader may need a moment with the",
   "No migration story of its own — Kysely's own migrator is deliberately not used, so that th",
   "Nothing prevents an implementer from wrapping the retry loop in a transaction. It is prohi",
   "Option A — pg (node-postgres) alone, with hand-written SQL strings and hand-written row ty",
   "Option B — Kysely over the pg driver: a typed query builder that compiles to SQL, with an",
   "Option C — Prisma.",
   "Option D — Drizzle ORM.",
   "Option E — TypeORM or Sequelize (a classic data-mapper/active-record ORM).",
   "The Database interface is a second statement of the schema beside the migrations, and noth",
   "The two statements that carry the invariant — the guarded INSERT and the guarded UPDATE — ",
   "This was the architect's first choice and is the closest runner-up. It loses only on goal ",
   "err.constraint survives, so the 409 names the contended resource (§1.3),",
   "§1.3, §8.4 — the refusal must name which resource was contended, and"
  ],
  "decision": "Chosen option: Option B — Kysely as a typed SQL builder over pg, with no ORM anywhere in the"
 },
 "0007-node-pg-migrate-with-sql-files.md": {
  "options": [
   "--",
   ".sql files carry no types, so nothing links a migration to the Database interface ADR-0006",
   "Applying migrations from the test fixture is a function call, so there is no \"did you reme",
   "Bad, because it cannot be invoked programmatically from the Testcontainers fixture without",
   "Bad, because it couples two decisions that ADR-0006 deliberately separated, so a future ch",
   "Bad, because it is a Go binary, so the test fixture must shell out to a tool that must be ",
   "Bad, because it is a framework rather than a tool: the PostgreSQL storage, the file resolv",
   "Bad, because it is exactly the kind of infrastructure that consumes human review attention",
   "Bad, because it is the tool's less-travelled path, so examples and answers found online wi",
   "Bad, because it makes the schema's source of truth a JavaScript program, which is precisel",
   "Bad, because its centre of gravity is Sequelize, so PostgreSQL-specific usage is off the b",
   "Bad, because the correctness properties a migration runner needs — a transactional state t",
   "Bad, because the objects that matter — the extension, two exclusion constraints with a par",
   "Bad, in the Kysely case, because Kysely's migrations are TypeScript modules — Option B's o",
   "Bad, in the drizzle-kit and prisma migrate cases, for the reason ADR-0006 rejected them: t",
   "Bad, or deferred",
   "Down migrations are written and never exercised, so they are unverified by construction. S",
   "Down migrations are written but are not part of any recovery story. They exist so a migrat",
   "Every migration is plain SQL. The DSL is not used, including for tables that could trivial",
   "Good",
   "Good, because it has no opinion about the query layer (requirement 2).",
   "Good, because it is a plain npm dependency with a Node API, so the Testcontainers fixture ",
   "Good, because it is a well-tested, storage-agnostic migration framework with a clean Node ",
   "Good, because it is completely transparent and adds no dependency.",
   "Good, because it is genuinely excellent at this job.",
   "Good, because it is one fewer dependency and one fewer concept, and the schema types would",
   "Good, because it is the most SQL-first option of all: one file, up and down, nothing else.",
   "Good, because the DSL is type-checked, composable, and generates the down migration for mo",
   "Good, because the SQL is the artifact and the runner is maintained by someone else — requi",
   "Good, because tools/ already contains bespoke scripts, so it would not be out of place.",
   "Independent of ADR-0006: swapping Kysely for something else touches no migration.",
   "Migrations are never edited after merge, on the same reasoning that makes ADRs immutable: ",
   "Migrations live in src/persistence/migrations/ as NNNNdescription.sql, applied in",
   "No schema-diffing and no drift detection: nothing notices if someone changes a database by",
   "Option A — node-pg-migrate, with migrations authored as plain .sql files.",
   "Option B — node-pg-migrate, with migrations authored in its JavaScript DSL (pgm.createTabl",
   "Option C — a hand-rolled runner: numbered .sql files and a schemamigrations table, applied",
   "Option D — dbmate: up/down in one .sql file, applied by a single Go binary.",
   "Option E — Umzug with a PostgreSQL storage adapter and raw-SQL migration bodies.",
   "Option F — the query layer's own migrator (kysely migrator, drizzle-kit, prisma migrate).",
   "The exclusion-constraint DDL of CLAUDE.md §2.1 appears in this repository exactly as writt",
   "The migration corpus is legible to anyone who reads SQL, with no knowledge of this project",
   "The runner is invoked programmatically (node-pg-migrate's Node API) both by",
   "node-pg-migrate is chosen partly for a feature (.sql file support) that is not its headlin"
  ],
  "decision": "Chosen option: Option A — node-pg-migrate executing plain .sql migration files."
 },
 "0008-module-decomposition.md": {
  "options": [
   "--",
   "---",
   "A-1 duration varies by vehicle",
   "A-10 / ADR-0009 candidate ordering or the cap changes",
   "A-3 technicians float between dealerships",
   "A-4 an occupancy buffer appears",
   "A-5 search-style \"any slot on Tuesday\"",
   "ADR-0001 opening hours grow (breaks, holidays, closures)",
   "Ambiguity containment — the goal-3 criterion, made concrete. Each row names the single mod",
   "Bad, because \"services\" is where policy and orchestration merge, so the pure, cheaply test",
   "Bad, because TC-7 requires the layering to be enforced in CI and there would be nothing to",
   "Bad, because a five-layer graph for four endpoints is more structure than the endpoint cou",
   "Bad, because it departs from the shape a reviewer expects, so it needs the paragraph above",
   "Bad, because it fails the goal-3 criterion outright: A-1's duration rule would have a frag",
   "Bad, because the ambiguity-containment property would be a claim about discipline, and §1.",
   "Bad, because the port's method signatures would have to leak PostgreSQL semantics anyway —",
   "Bad, because the shared kernel that inevitably forms to fix that is Option D arrived at by",
   "Bad, because the unit-testability it buys is testability of the layer where nothing intere",
   "Bad, because the §1.4 ambiguities cut across features, not along them: interval derivation",
   "Bad, decisively, because the port would be substitutable and the invariant is not. An in-m",
   "Bad, or deferred",
   "Five modules is small enough that the generated dependency graph (§5.3) fits on a page and",
   "GC-2 authentication arrives",
   "Goal 3's claim is checkable rather than asserted: §10 carries a scenario that fails if",
   "Good",
   "Good, because at four endpoints it would work, and it would be the least code.",
   "Good, because domain-is-pure turns GC-1's most important prohibition — the opening-hours r",
   "Good, because feature-local changes are genuinely well contained.",
   "Good, because it is the standard answer for a system with real domain logic, and a reviewe",
   "Good, because it matches the slice loop, so one slice touches one folder and merges cleanl",
   "Good, because it needs no explanation and every reader has seen it.",
   "Good, because it would make the application layer unit-testable without Docker.",
   "Good, because the dependency rules are trivial to write and to enforce.",
   "Good, because the goal-3 criterion is met for four of five predicted changes, and the fift",
   "Good, because the layer boundaries and the test-ownership boundaries (§5 of CLAUDE.md) lin",
   "No repository port means no seam for a second persistence technology. Deliberate, and it w",
   "Option A — technical layering: routes/, services/, repositories/, models/.",
   "Option B — hexagonal / ports and adapters: a persistence-ignorant domain owning a",
   "Option C — vertical feature slices: features/booking/, features/reschedule/,",
   "Option D — layered modules around a dependency-free policy core, with the layer boundaries",
   "Option E — no decomposition: one src/ of cohesive files, layering by convention.",
   "Rule",
   "The application layer's tests all need Docker, so a Docker-less environment can run only t",
   "The architecture's central honesty — persistence is not swappable here — is visible in the",
   "The core has no dependencies, so the domain test suite runs in milliseconds and is where m",
   "The five rules that are enforced (.dependency-cruiser.js, and §5.3):",
   "Why application depends on persistence concretely, with no port. This is the deliberate",
   "domain-is-pure",
   "http may import domain types. That edge is allowed for response shapes and is not enforced",
   "http-must-not-reach-persistence",
   "http-only-in-the-edge",
   "outside-in-tests-do-not-import-src",
   "sql-only-in-persistence",
   "src/platform is the module most likely to become a junk drawer, because \"importable by eve",
   "§1.4 item"
  ],
  "decision": "Chosen option: Option D. Five modules, one permitted dependency direction, and a core that is"
 },
 "0009-candidate-ordering-and-attempt-cap.md": {
  "options": [
   "--",
   "16 attempts, after which the request is refused exactly as if the list were exhausted.",
   "Bad, because \"fairest allocation\" is scheduling policy, and §3.3 excludes scheduling polic",
   "Bad, because a concurrency test that fails cannot be re-run on the interleaving that faile",
   "Bad, because a global RNG inside src/domain would break domain-is-pure (ADR-0008), so the ",
   "Bad, because concurrent requests computing utilisation from the same snapshot agree with e",
   "Bad, because it concentrates all contention on the lowest-sorting resource, which is also ",
   "Bad, because it couples the loop to constraint naming, so a migration can change behaviour",
   "Bad, because it is more machinery than either neighbour: a seed must be threaded from the ",
   "Bad, because it is precisely the failure ADR-0004 named: every concurrent request tries th",
   "Bad, because it is theoretically over-eager if a conflicting appointment is cancelled mid-",
   "Bad, because it needs an aggregate over the day's appointments on the booking path, which ",
   "Bad, because it re-attempts pairs already known to be impossible — a bay observed to be ta",
   "Bad, because the bound is multiplicative, so the cap must be large or the bound must be tr",
   "Bad, because the seed's variability is an untested assumption (see above).",
   "Bad, or deferred",
   "Bound-1 — attempt pairs one at a time, prune only the pair that failed. Worst case",
   "Bound-2 — prune by the constraint that fired: a nobayoverlap violation eliminates that bay",
   "Concurrency tests are reproducible: a seeded order plus a recorded seed makes a failing in",
   "Concurrent requests spread across candidates instead of queueing on one, so the retry work",
   "Good",
   "Good, because it also spreads contention, since the least-loaded resource differs between ",
   "Good, because it forces the design to use information the database already gives it, which",
   "Good, because it is completely reproducible with no seed plumbing, and the simplest thing ",
   "Good, because it keeps ordering a pure function, which is what lets it live in src/domain ",
   "Good, because it makes resource allocation predictable, which a service manager might actu",
   "Good, because it needs nothing from the error beyond its SQLSTATE, so it survives a query ",
   "Good, because it produces the outcome the service manager stakeholder (§1.3) actually want",
   "Good, because it spreads contention as well as anything can, with no seed to manage.",
   "Good, because it spreads contention like Order-B and reproduces like Order-A.",
   "Good, because it turns a multiplicative bound into an additive one for the price of readin",
   "Order-A — deterministic by id. Stable, reproducible, identical across concurrent requests.",
   "Order-B — uniform random shuffle, freshly randomised per request.",
   "Order-C (seeded shuffle), Bound-2 (prune by the constraint that fired), and a hard cap of ",
   "Order-C — seeded shuffle: a deterministic permutation from a per-request seed, with the se",
   "Order-D — load-balancing: order by fewest appointments that day, so work spreads across",
   "Ordering",
   "Ordering is a pure function of (candidates, seed) in src/domain/candidates.ts, so it is",
   "Pruning consumes information from err.constraint, so the constraint names in the migration",
   "The attempt bound is additive, which makes a small cap meaningful. Under Bound-1 the cap w",
   "The cap can still produce a refusal while capacity remained — the residual spurious refusa",
   "The seed must actually vary. A misconfigured deployment that seeds every request identical",
   "Traversal and bound",
   "Work is not balanced across resources: a seeded shuffle spreads contention but does not sp",
   "bays",
   "nobayoverlap → drop that bay from the bay list.",
   "notechnicianoverlap → drop that technician from the technician list."
  ],
  "decision": ""
 },
 "0010-github-actions-and-check-run-collection.md": {
  "options": [
   "",
   "--",
   "---",
   "Bad, because CLAUDE.md §2.3 requires layering violations to fail the build; locally that i",
   "Bad, because METHODOLOGY §261 then permits no slice to leave red. The loop stops at step 3",
   "Bad, because OC-2 makes verifiability a graded quality goal. \"I ran the tests\" is the self",
   "Bad, because check.run could never carry tier derived (write.mjs refuses it from the",
   "Bad, because it is a machine to maintain and secure, against OC-1's time box and OC-3's si",
   "Bad, because it is a second account, a second set of credentials, and a second thing to ex",
   "Bad, because it re-introduces exactly what CI was adopted to remove: evidence produced on ",
   "Bad, because results arrive on the pull request through an integration rather than being p",
   "Bad, because runner performance varies between runs, which makes QS-14 a noisier signal th",
   "Bad, because the YAML and the red-proof inversion are GitHub-specific.",
   "Bad, because the repository is on GitHub. Mirroring makes CI report on a copy; moving disc",
   "Bad, or deferred",
   "Branch stays the author's",
   "CLAUDE.md §2.3",
   "CLAUDE.md §2.3 — \"Violations fail the build. Architecture conformance is not a matter of",
   "CLAUDE.md §2.4, §6 step 3",
   "Deliberately not in CI, with reasons, because a claim that CI enforces something it does n",
   "Docker-in-CI is unproven here. Testcontainers on ubuntu-latest is well-trodden, but nothin",
   "Good",
   "Good, because GITHUBTOKEN is scoped per run and needs no secret to be created or rotated.",
   "Good, because hardware is fixed, which is worth something to QS-14, and Docker is whatever",
   "Good, because it is free, has no latency and no vendor.",
   "Good, because remote Docker support is mature and configuration is compact.",
   "Good, because the YAML is arguably cleaner and services are first-class.",
   "Good, because the checks live on the pull request, which METHODOLOGY §8 already designates",
   "Good, because the run history is queryable by gh with the credentials the human already ha",
   "Good, because ubuntu-latest ships Docker, which TC-9 makes a correctness prerequisite.",
   "Link integrity, ADR existence, QS- → real test. METHODOLOGY §4 puts these in the enforced",
   "Log is complete offline",
   "METHODOLOGY §261 becomes satisfiable: a slice can legally leave red, which it cannot today",
   "METHODOLOGY §261 — no CI, no recording of the red state, so no slice may leave red. This i",
   "METHODOLOGY §4",
   "METHODOLOGY §7 (§261)",
   "METHODOLOGY §9 (§400)",
   "OC-2 / METHODOLOGY §9 — evidence must be derivable rather than narrated. A test run on the",
   "Option A — GitHub Actions on GitHub-hosted ubuntu-latest runners.",
   "Option B — GitHub Actions on a self-hosted runner (the human's machine, or a VM).",
   "Option C — CircleCI, connected to the same GitHub repository.",
   "Option D — GitLab CI, with the repository mirrored or moved.",
   "Option E — no CI; every check run locally before merge. The status quo.",
   "Phase 4, when src/ and the suite exist: typecheck · lint:arch (QS-10) · the Vitest suite o",
   "QS-14's budget is now tied to a runner class. It is stated \"on the CI container\", so the",
   "Recommended, not decided: add .npmrc with engine-strict=true, which makes the same rule bi",
   "Record exists the instant CI finishes",
   "Rejected on the same ground as C, more strongly: the repository would have to move to suit",
   "Rejected. It is the only option that breaks named, NON-NEGOTIABLE rules rather than being ",
   "Rejected: it costs more and buys less evidence.",
   "Retention, in decreasing durability, since the test is green a day later:",
   "Rule",
   "Step",
   "Stryker. METHODOLOGY §7 makes mutation survivors the reviewer's findings. Mutation testing",
   "TC-10 acquires the enforcement point it was recorded as lacking.",
   "The log lags CI between gates. Accepted in Decision 2, and the direct cost of durability.",
   "The property being bought is durability, and what is paid for it is immediacy. The event l",
   "The pull request stays the gate artifact (METHODOLOGY §8): checks, review and the human's ",
   "The red proof depends on a commit-message convention. It is NON-NEGOTIABLE (CLAUDE.md §7) ",
   "The workflow needs only contents: read. Nothing in CI can write to the repository whose hi",
   "Three documentation claims move from written to enforced (assembly currency, diagram expor",
   "Tier derived is honest",
   "Today — one job, verify, all of it independent of src/:",
   "Token scope needed",
   "Two checks live as inline shell and node -e in YAML, which is untested code in a repositor",
   "Vendor coupling. The red-proof mechanism, the artifact retention and the collector all spe",
   "arc42 §10, QS-10",
   "arc42 §10, QS-14",
   "between gates.",
   "check.run gets a mechanism with an honest derived tier — the fact is computed by a collect",
   "check.run is still not emitted. This ADR decides the mechanism; collect-ci.mjs and the",
   "compile before the implementation existed, and a compile failure is indistinguishable from",
   "docs/team-log/events.jsonl gains lines and never loses or alters one",
   "every docs/diagrams/.html has a committed .svg, and every .svg referenced from arc42 exist",
   "every other check — install, typecheck, lint, dependency-cruiser, docs, tools, unit — must",
   "every record in the log validates against tools/team-log/schema.mjs",
   "npm ci --engine-strict",
   "npm run docs:check",
   "npm run log:audit. It cannot run in CI at all. Its ground truth is",
   "npm run test:tools",
   "red-proof run and asserts its shape:",
   "selfcheck.py / verify-geometry.py. METHODOLOGY §4 states these run in CI. They currently",
   "the acceptance suite must exit non-zero, and",
   "ubuntu-latest ships a working Docker daemon, which TC-9 requires.",
   "unable to legally advance past step 3: with no CI there is no recording of the red state, "
  ],
  "decision": "Chosen option: Option A — GitHub Actions on GitHub-hosted ubuntu-latest runners, because"
 },
 "0011-health-is-an-operational-probe.md": {
  "options": [
   "--",
   "Bad, because /problems/service-degraded sits in a table whose other seven rows are decided",
   "Bad, because AC-2 of slice 00a requires the endpoint, and acceptance criteria are the huma",
   "Bad, because it doubles the surface of the one endpoint in a slice whose purpose is to pro",
   "Bad, because it puts an operator concern into a client contract, and TC-5's stubbed client",
   "Bad, because nothing in §7's deployment reads either signal — no orchestrator, no load bal",
   "Bad, because the exclusion from OpenAPI is unenforced until slice 10.",
   "Bad, because the service now has two response conventions, and the one a reader meets firs",
   "Bad, because the walking skeleton would then have no endpoint at all, and nothing would pr",
   "Bad, because §1.4 and §3.1 give the operator \"can tell whether it is healthy\" as a goal, a",
   "Bad, or deferred",
   "Deferred rather than rejected: superseding this ADR is the route back to it.",
   "Good",
   "Good, because it adds no surface at all and the information is available to anyone with th",
   "Good, because it is the correct long-run shape and the one an orchestrator expects.",
   "Good, because liveness stops depending on PostgreSQL, so a database outage cannot cause a ",
   "Good, because the emitted contract keeps matching §3's five operations with no exception t",
   "Good, because the probe body can evolve freely — no contract test pins it.",
   "Good, because the probe is documented for whoever has to operate the service.",
   "Good, because there is exactly one error convention in the whole service, which is the sim",
   "Good, because §8.6's taxonomy stays uniform: every entry a domain outcome with a named dec",
   "Is /health in docs/api/openapi.json? ADR-0005 emits from the route table, so by default ye",
   "Is a health probe a traced business operation? Under load a probe every few seconds would ",
   "Is that 503 an RFC 9457 problem document with a type of /problems/…? §8.6's table has no r",
   "Liveness is unavailable. With one endpoint that touches the database, a service that is ru",
   "Nothing enforces exclusion from the OpenAPI document until slice 10 writes the emitter and",
   "Option A — an operational probe, outside the contract. Plain JSON body on both 200 and 503",
   "Option B — a sixth operation, inside the contract. 503 rendered as application/problem+jso",
   "Option C — split: /health (liveness, no dependencies) and /ready (readiness, checks the",
   "Option D — no endpoint; connectivity is observed from logs and metrics only.",
   "Slices 03, 09 and 10 inherit an answer instead of each re-deriving one.",
   "The 503 has no machine-readable failure type, so a sophisticated operator client cannot",
   "The emitted contract keeps saying five operations, agreeing with §3 and §8.6 without a cav",
   "The probe stays cheap to change: nothing external is promised about its body, so adding",
   "Two response conventions in one service. A reader meeting /health first will see plain JSO",
   "architecture: http-must-not-reach-persistence applies unchanged, and the walking skeleton'",
   "domain outcome decided by a named module or by PostgreSQL.",
   "errors are RFC 9457 application/problem+json with a stable type per failure. ADR-0005 stat",
   "§8.6's taxonomy keeps the property that makes it worth testing (QS-11): every entry is a d"
  ],
  "decision": "Chosen option: Option A — /health is an operational probe outside the API contract."
 },
 "0012-seed-fixtures-are-a-test-owned-loader.md": {
  "options": [
   "--",
   "A second, independent statement of the reference schema exists, and it fails loudly on div",
   "Bad, because a demo dataset built before any endpoint is a guess about what the demo will ",
   "Bad, because a single file still means a single dealership, so Option A's isolation object",
   "Bad, because it duplicates knowledge of the reference schema in a second place — mitigated",
   "Bad, because it gives the whole suite one dealership. Isolation by data collapses, the sui",
   "Bad, because it is not in slice 00's In scope, and building it means fixing the demo narra",
   "Bad, because it leaves the demo with no dataset, which Option E fixes at a cost.",
   "Bad, because it puts fixtures into the schema that runs in the local compose stack, which ",
   "Bad, because it puts test fixtures into a corpus ADR-0007 makes immutable after merge, so ",
   "Bad, because outside-in-tests-do-not-import-src forbids tests/support/ from importing it, ",
   "Bad, because the fixtures the acceptance suite books against would then be authored and ow",
   "Bad, because the test-engineer would be executing a file it is forbidden to read. Independ",
   "Bad, or deferred",
   "Every case owns a disjoint subtree, so every row in the table is attributable to the case ",
   "Every id in a failure message is a pure function of the case's name, so a failing row can ",
   "Good",
   "Good, because it evades the import rule legitimately: tests/architecture/layering.test.ts ",
   "Good, because it forces the demo's stable ids to be decided while the schema is fresh.",
   "Good, because it is an independent transcription of §8.1 and therefore detects column-leve",
   "Good, because it is trivially \"loadable into an empty database\", which is AC-9's literal w",
   "Good, because it is where a reader would look for it.",
   "Good, because it satisfies both the isolation model and the read boundary, which no other ",
   "Good, because one artifact serves both the suite and the demo, and it is typed against the",
   "Good, because the demo story is complete from the first slice, and a reader can curl again",
   "Good, because the fixture stays plain SQL, consistent with ADR-0007's argument for the mig",
   "Good, because the suite needs no seeding code at all: the container arrives populated.",
   "Ids are derived, not random and not literal. uuidFor(namespace, name) hashes the pair, so",
   "Isolation is genuinely partial. servicetype, customer and vehicle carry no",
   "No ON CONFLICT DO NOTHING. Two cases sharing a namespace must fail loudly on the primary k",
   "No extension is needed for id generation: ids come from the application side, so btreegist",
   "Option A — a fourth migration, 0004seed.sql. Reference rows arrive with the schema, applie",
   "Option B — a loader module under src/persistence/seed/, exported and called by the test",
   "Option C — a checked-in .sql fixture under src/, executed by path from the test, the way",
   "Option D — a loader in tests/support/seed.ts, owned by the test-engineer, issuing raw SQL",
   "Option E — Option D for the suite, plus a separate src/persistence/seed/ dataset and",
   "Rows accumulate for the life of a run and are never cleaned up. Acceptable at one containe",
   "The demo dataset is not built. When it lands it is the implementer's, under",
   "The fallback is withdrawn. Recorded because the refusal improved the decision, which is wh",
   "The fixtures that define done are authored by the role that defines done, from the",
   "The loader duplicates knowledge of the reference schema. That duplication is the independe",
   "The reason is diagnostic, not cosmetic, and it is the test-engineer's rather than the",
   "There is no demo dataset, so nothing yet supports a cURL example with a stable id in its U",
   "Two seed artifacts will eventually exist with overlapping shape and different owners, and",
   "extra column.",
   "tests/support/seed.ts exposes seedDealership(client, namespace), which inserts one"
  ],
  "decision": "Chosen option: Option D, with Option E's second half deferred — the suite's fixtures are a"
 },
 "0013-outside-in-tests-exercise-the-built-artifact.md": {
  "options": [
   "\"a real assertion failure rather than a missing import\", and red-proof's first preconditio",
   "--",
   "A dependency on the build. A stale dist/ produces a green or a confusing red. pretest and",
   "A filename convention (.db.test.ts) is a weaker guarantee than a directory the tooling enf",
   "A property test that needs the database is named .db.test.ts. Everything else under",
   "An outside-in test may load a compiled artifact under dist/. It may not import src/ by any",
   "Bad, because QS-9 would lose its outside-in status, which is most of what makes it evidenc",
   "Bad, because a property deferred past the slice that builds its subject is a property writ",
   "Bad, because dependency-cruiser cannot see the load, so the rule's coverage is narrower in",
   "Bad, because it converts an absolute rule into a list. domain-is-pure's own comment gives ",
   "Bad, because it obeys the rule's text while defeating its mechanism — the test reaches int",
   "Bad, because it starts a database to test three functions that import nothing, which is th",
   "Bad, because it would be the first place in this codebase where a rule is satisfied by con",
   "Bad, because the DST rule — the substance of slice 01 and the thing ADR-0001 flagged as \"t",
   "Bad, because the loaded module is untyped and the test depends on the build being current.",
   "Bad, decisively, and measured: a globalSetup abort in either project discards the other's",
   "Bad, decisively: it does not solve the problem. tsc still fails on a module that does not ",
   "Bad, decisively: it makes this slice's red evidence destroyable by a Docker failure, conve",
   "Bad, decisively: tests/unit/ is the implementer's under CLAUDE.md §5. The definition of do",
   "Bad, or deferred",
   "Found by measuring. The remedy that suggests itself — widen the rule for tests/property/ →",
   "Found by reading. .dependency-cruiser.js forbids every test-engineer-owned directory, prop",
   "Good",
   "Good, because it extends a convention the project already relies on rather than inventing ",
   "Good, because it is the simplest thing that compiles: unit tests may import src/ and are e",
   "Good, because it is the status quo and needs no edit.",
   "Good, because it is the status quo, needs no new tooling, and keeps one command as the who",
   "Good, because it needs no build and no ruleset change, and it typechecks.",
   "Good, because it needs no new mechanism: by then there is a real boundary and a real repos",
   "Good, because it produces test-results.json with no merge step, and a merge step is a new ",
   "Good, because it would also cover AC-4's end-to-end form and the 400 mapping, which Option",
   "Good, because it would feed the mutation score, which no other option does.",
   "Good, because one home for all property tests is simpler to explain than two.",
   "Good, because static imports keep the tests type-checked against the domain.",
   "Good, because the dependency would be visible in the graph and honestly declared rather th",
   "Good, because the guarded load turns \"not implemented yet\" into an assertion with a messag",
   "Good, because the test exercises the same artifact a deployment runs, including whatever t",
   "It must assert the loaded module's shape — every export it uses, present and of the right ",
   "Option A — Widen outside-in-tests-do-not-import-src to allow tests/property/ → src/domain/",
   "Option B — Computed dynamic import of src/domain/.ts, so tsc cannot resolve the specifier",
   "Option C — Load the built artifact dist/domain/.js through a computed dynamic import, guar",
   "Option D — Move the test to tests/unit/, the implementer's directory, and drop QS-9's",
   "Option E — Defer QS-9 to an HTTP-level property at the slice that adds the booking route.",
   "QS-9 becomes executable in the slice that builds its subject, rather than several slices l",
   "The db/no-db question is settled before the slice that adds QS-8's constraint-agreement pr",
   "The loaded module is any. The outside-in tests lose static typing against the domain, repl",
   "The narrowing recorded above was itself overstated, and the overstatement is the architect",
   "The property test stops depending on Docker, so a container failure cannot destroy this sl",
   "The red commit's failure is an assertion with a readable message — \"dist/domain/openingHou",
   "The residue is irreducible for a text scan, and is recorded as such rather than deferred t",
   "The second clause was published claiming a protection it did not provide, and it took a",
   "The specifier is computed because of C1, not to hide from dependency-cruiser. That order m",
   "The two absolute rules stay absolute. Nothing becomes a list.",
   "Third clause — the invocation split. npm test becomes tools/ci/run-tests.mjs, which:",
   "What changed. The second clause originally said that splitting tests/property/ between the",
   "Whatever runs an outside-in test must guarantee a current build. npm test already does thr",
   "Worth keeping as the fallback if the human rejects Option C, and the cost of that fallback",
   "constructing a path to src/ from importing from src/, because at the level it reads the so",
   "dependency-cruiser cannot see a computed dynamic import. The same technique would let a fu",
   "dependency-cruiser catches no computed form at all, which was already recorded and is",
   "it catches neither root-anchored path construction — join(ROOT, 'src', 'domain', …) — nor",
   "relative path (../src/), which is the form such a test reaches for first and which the cru",
   "splitting tests/property/ by database need, on the naming convention .db.test.ts for the",
   "the scan catches relative ../src/ references — the SRCREFERENCE pattern — in the seven",
   "therefore the hole is narrowed, not closed, and the residue is review.",
   "vitest.mutation.config.ts stays tests/unit/ only. It is not extended to property tests, fo",
   "~~One mechanical unknown~~ — measured before this ADR was accepted, not left as a promise:"
  ],
  "decision": "Chosen option: C — outside-in tests reach a pure module through the built artifact — together with"
 },
 "0014-an-instant-is-renderable-by-construction.md": {
  "options": [
   "--",
   "Bad, because a precondition nobody can check is a comment, and this codebase has already r",
   "Bad, because a try/catch around a formatToParts call cannot distinguish \"the instant is ou",
   "Bad, because control flow by exception in a function whose design property is purity reads",
   "Bad, because instant() keeps minting values that violate their own type's implied contract",
   "Bad, because it leaves instant() untouched, so it inherits every objection to Option B.",
   "Bad, because it treats the symptom's location as the defect's location. The defect is that",
   "Bad, because of the duplicated literal, which is real and is booked as debt rather than de",
   "Bad, because the first caller to hit it gets a 500 from the module whose entire design cla",
   "Bad, because two checks for one property invites a future reader to delete \"the redundant ",
   "Bad, decisively: it puts a domain invariant in the edge. Any caller that is not an HTTP re",
   "Bad, decisively: withinOpeningHours cannot receive an Instant under the literal AC-6 rulin",
   "Bad, or deferred",
   "Good",
   "Good, because each site is justified independently: the constructor states what the type m",
   "Good, because it costs nothing now.",
   "Good, because it is one branch in one file, and the smallest change that makes the brand m",
   "Good, because it is robust to the AC-6 ruling being revisited in either direction. If doma",
   "Good, because it keeps a platform limit out of the pure core.",
   "Good, because it makes the function total with no arithmetic and no magic constant, and it",
   "Good, because it needs no change to interval.ts and no thinking about brands.",
   "Good, because it produces the best HTTP behaviour: a 400 with a useful message, at the edg",
   "Good, because it protects the only function that can currently throw, which is where the o",
   "Good, because it puts the invariant where the type is minted, so it holds for every consum",
   "Good, because it would also catch any other Intl failure mode nobody has thought of.",
   "Good, because nothing is broken today and no criterion is violated, which is precisely why",
   "It is directly testable at both sites, and the boundary values (±8640000000000000 and",
   "It needs its own red commit under §2.4, which is why it is a backlog slice rather than a p",
   "Not part of this decision, but stated so the next slice does not re-derive it. The HTTP re",
   "Nothing is broken today. No AC, no quality scenario and no §2 invariant is violated by the",
   "Option A — bound it in instant() only.",
   "Option B — bound it in withinOpeningHours step 1 only.",
   "Option C — bound it in instant() AND in withinOpeningHours step 1.",
   "Option D — bound it in the HTTP request parser only.",
   "Option E — catch the RangeError inside withinOpeningHours and return malformed-interval.",
   "Option F — do nothing; document the range as a precondition.",
   "The bound is a JavaScript Date limit leaking into domain policy. It is honest — the system",
   "The brand becomes evidence of the property consumers actually need. An Instant is renderab",
   "The duplication is deliberate and is booked as debt, not hidden. The literal",
   "The failure moves from a RangeError deep inside Intl to a null at the constructor or a",
   "The literal is duplicated across two domain files with no way to share it. See D-01-2 abov",
   "The type is branded, and a brand is a promise. Instant exists as number & { brand } with",
   "Two more branches to cover, in a slice whose mutation threshold is already the strictest i",
   "instant() is the primary home, and the reason is what the type is for. Instant's construct",
   "withinOpeningHours cannot be handed an Instant. Under the human's literal AC-6 ruling no",
   "withinOpeningHours stays total. Its documented verdict list becomes the complete descripti",
   "withinOpeningHours step 1 gets it too, and this is a consequence of the AC-6 ruling rather"
  ],
  "decision": "Chosen option: C — the bound is applied in both places, instant() primarily and"
 },
 "0015-an-interval-ending-at-local-midnight-does-not-span-two-days.md": {
  "options": [
   "--",
   "A dealership open until midnight can take its last appointment. Today it cannot take any",
   "A special case in the one function in this system that is already the hardest to reason ab",
   "Bad, and decisively, because it changes step 7's boundary from inclusive on closesAt to so",
   "Bad, because \"skip the check\" and \"the check passes\" are different things, and only one of",
   "Bad, because 86400 is out of range for a field named seconds-of-day.",
   "Bad, because it adds a conditional to the system's most DST-sensitive function.",
   "Bad, because it leaves an unkillable branch in place permanently, so every future mutation",
   "Bad, because it leaves the actual defect — a midnight-ending job rejected as spanning two ",
   "Bad, because it makes the verdict depend on the millisecond resolution of the endpoint. A ",
   "Bad, because it removes an assertion rather than correcting one, which is the failure mode",
   "Bad, because it would delete the correct outcome of the implementer's own measurement in o",
   "Bad, because the '24:00:00' parser arm stays unreachable — a dealership closing at '24:00:",
   "Bad, because the documentation would be untrue to ADR-0001: \"the whole derived interval mu",
   "Bad, decisively: PostgreSQL accepts '24:00:00'::time and round-trips it — measured against",
   "Bad, or deferred",
   "Explicitly not part of this decision. Whether the occupancy interval (A-4, with a buffer) ",
   "Good",
   "Good, because it avoids the out-of-range 86400 encoding entirely.",
   "Good, because it costs nothing and risks nothing in the function most expensive to get wro",
   "Good, because it fixes the defect at the point where the wrong assumption is made — step 4",
   "Good, because it is arguably the most honest statement of what half-open means: the interv",
   "Good, because it is the smallest change, it removes the unkillable mutants immediately, an",
   "Good, because it makes the domain agree with the tstzrange semantics the constraint enforc",
   "Good, because it needs no special case and no out-of-range encoding — every value stays in",
   "Good, because it retires the dead branch by making it live, which is strictly better than ",
   "Good, because nothing is broken for any dealership in the seed data, no acceptance criteri",
   "It needs a red commit and a QS-9 generator extension, so it is a slice rather than a line.",
   "It needs its own red commit, under CLAUDE.md §2.4, and a property extension for QS-9 that",
   "Option A — normalise the endpoint: an end rendering as 00:00:00 on the local day immediate",
   "Option B — compare the last instant of the interval (endsAtMillis - 1) instead of endsAt.",
   "Option C — delete the '24:00:00' parser arm and reject the value.",
   "Option D — leave the behaviour; document spans-local-days as covering midnight-ending jobs",
   "Option E — allow the date to differ whenever the end renders as 00:00:00, and skip the",
   "The 86400 normalisation is directly testable at the boundary — 23:59:59.999, 24:00:00.000 ",
   "The condition has two clauses and the second is easy to drop. A test that only exercises t",
   "The condition is deliberately narrow. Renders as exactly 00:00:00 and on the local date",
   "The domain's interval semantics stop disagreeing with the exclusion constraint's, which re",
   "The reported symptom, and why the symptom is not the defect. The reviewer found this as a ",
   "The unreachable parser arm and its unkillable mutants go away as a side effect, which mean",
   "Why the branch must not be deleted, which is the whole reason this is an ADR. The obvious ",
   "one day's opening hours, and no weekly schedule can express an interval that spans two. Th",
   "secondsOfDay = 86400 is a value outside the range its own name implies. It is the standard"
  ],
  "decision": "Chosen option: A — an end rendering as 00:00:00 on the local day immediately following the start's"
 },
 "0016-a-capacity-refusal-requires-a-database-verdict.md": {
  "options": [
   "--",
   "---",
   "A brand on a two-member string union is unusual enough that a reader may take it for cerem",
   "A cast defeats it, measured. The residue is a scan plus review, and the scan is a text sca",
   "A dealership with no bays or no qualified technicians produces an empty candidate set and ",
   "And the escape hatch was measured too, because a mechanism's limits are part of the mechan",
   "Bad, because \"fabricate a plausible error object\" is barely harder than \"write a cast\", so",
   "Bad, because a text scan cannot see a computed table name, an interpolated identifier or a",
   "Bad, because it asserts on telemetry, so deleting a span deletes the guard, and nothing wo",
   "Bad, because it is a test that can only exist from slice 09, and this slice is where the i",
   "Bad, because it is one more brand in a codebase that already had to write an ADR about a b",
   "Bad, because it puts a persistence-shaped value into an application-layer outcome and then",
   "Bad, because of the cast escape hatch, measured and recorded rather than denied.",
   "Bad, because the nearest expressible rule — forbid src/application → candidateRepository —",
   "Bad, decisively: dependency-cruiser reasons about module edges. Check-then-act is not an e",
   "Bad, decisively: it is the only mechanism, and it is the weakest available kind, for the r",
   "Bad, or deferred",
   "Good",
   "Good, because AC-3 and AC-4 assert on the constraint name, which this makes available with",
   "Good, because a scan catches the shape — a NOT EXISTS against appointment in the wrong fil",
   "Good, because it is checked by npm run typecheck, which runs on every commit, rather than ",
   "Good, because it is strictly more evidence: the outcome would hold constraint and the orig",
   "Good, because it is what AC-5 already asks for and it costs nothing to build.",
   "Good, because it would catch the defect behaviourally, which is the only thing no other op",
   "Good, because layering rules are already the project's enforcement idiom and CI already ru",
   "Good, because slice 09 builds the span and the metric anyway (QS-13), so the observation p",
   "Good, for the reasons in Decision, and because it puts the guarantee at the type level whe",
   "It adds a type-only dependency src/application → src/persistence. That edge is already per",
   "It applies to no-capacity and to nothing else. unknown-reference, vehicle-not-owned and",
   "It cannot be tested by the test suite that matters. Its evidence is a tsc exit code over a",
   "It composes with the layering already enforced. sql-only-in-persistence means the minting ",
   "It makes the empty-candidate-set case get a better answer than it would otherwise have got",
   "Not rejected, deferred: QS-13 already requires bookingconflictstotal to distinguish absorb",
   "Option A — do nothing structural; rely on AC-5's source scan and reviewer attention.",
   "Option B — a dependency-cruiser rule.",
   "Option C — brand the contended resource, minted only by the SQLSTATE classifier, and make",
   "Option D — carry the whole PgOutcome conflict object into the no-capacity outcome.",
   "Option E — assert at runtime that a 409 was preceded by a 23P01, e.g. through the span or",
   "Partially adopted: constraint is carried on the PgOutcome conflict variant, because AC-3 a",
   "The failure mode is the best available kind: a compiler error at the exact line, naming th",
   "The guard is free at runtime: a branded type is erased entirely.",
   "The honest answer today is: nothing. And that is the fact this decision exists for.",
   "The most important invariant in the system acquires a compile-time guard, in a place where",
   "Those two are the same string today, and nothing distinguishes them. resource: 'bay' is",
   "Tree",
   "pgError.ts + a use case refusing from a PgOutcome",
   "the same, plus a planted if (!free) return { kind: 'no-capacity', resource: 'bay', attempt"
  ],
  "decision": "Chosen option: C — ContendedResource is a branded type minted only inside"
 },
 "0017-the-composite-ownership-fk-is-disambiguated-after-it-fires.md": {
  "options": [
   "\"Adding the singleton foreign keys for tidiness would be redundant AND would make the REPO",
   "\"Vehicle not owned by the named customer | composite FK (23503) | reference data | 422\".",
   "- A-6 / ADR-0002: the vehicle belongs to the named customer. Validation, not authorisation",
   "--",
   "---",
   "422",
   "A concurrent delete between the insert and the classification would produce a verdict desc",
   "A refusal costs two round trips. Acceptable here, deliberately: it is a 4xx path, and §1.2",
   "AC-9 and AC-10 become distinguishable, which is the requirement, and QS-11's \"no two rows ",
   "Bad, because ADR-0002 makes the ownership rule a named validation failure on purpose — it ",
   "Bad, because DETAIL is localised prose. It changes with lcmessages, and it is not a docume",
   "Bad, because it adds a branch to a failure path and a read that has to be justified every ",
   "Bad, because it degrades the API for a service advisor, who can act on \"that vehicle is no",
   "Bad, because it is a migration in a slice whose data-model delta is otherwise zero, changi",
   "Bad, because it makes the composite FK's 23503 arm unreachable, which is R-01-4's exact sh",
   "Bad, because it still cannot separate the three cases: all three produce the same sentence",
   "Bad, because of the theoretical concurrent-delete misclassification above.",
   "Bad, because slice 00's migration explicitly rejected exactly this, on exactly this reason",
   "Bad, because the FK would then be defence-in-depth only, and defence-in-depth that never f",
   "Bad, decisively: AC-10 requires /problems/vehicle-not-owned and AC-9 requires",
   "Bad, decisively: it moves the decision away from where arc42 §6.6 and §8.6 both put it, an",
   "Bad, decisively: it trades a deterministic problem (three failures, one name) for a",
   "Bad, decisively: it would put string-parsing of a driver's human-readable output inside",
   "Bad, or deferred",
   "Failure",
   "Good",
   "Good, because arguably \"the vehicle is not the customer's\" is an unknown (vehicle, custome",
   "Good, because it costs the happy path nothing.",
   "Good, because it is the simplest and most familiar shape, and it gives a 422 in one round ",
   "Good, because it matches how opening hours are already handled — a validation read before ",
   "Good, because it needs no application logic: err.constraint would map straight to a proble",
   "Good, because it needs no read, no branch and no ADR, and the client still gets a 422.",
   "Good, because the FK stays live, so the constraint the migration argued for keeps doing wo",
   "Good, because the database would then report three distinct constraint names and no extra ",
   "Good, because the information genuinely is there — Key (vehicleid, customerid)=(…) is not ",
   "Good, for the three reasons in Decision: after the write, cannot permit, reference data on",
   "It is a read on the booking path, and every read on this path has to be argued about rathe",
   "Option A — validate ownership with a read before the insert; treat any 23503 as 500.",
   "Option B — classify with a read after the insert is refused, on",
   "Option C — add singleton foreign keys on customerid and vehicleid so the three failures",
   "Option D — collapse the two taxonomy rows: return /problems/unknown-reference for all thre",
   "Option E — parse the DETAIL line of the PostgreSQL error.",
   "OwnershipVerdict's three-member type is itself a small structural guard: the absence of 'o",
   "Status",
   "The classification statement is a fourth place that names customer and vehicle, after the",
   "The composite FK's arm stays live and its mutants stay killable, so the ownership constrai",
   "The database stays the decider for ownership, exactly as §6.6 and §8.6 specify. The classi",
   "The first three are the same error. The DETAIL line differs — it names the failing key pai",
   "The happy path pays nothing. The extra statement runs only when an insert has already fail",
   "arc42 already says which way this goes, and arc42 is the source of truth (CLAUDE.md §4). §",
   "both real, vehicle owned by a different customer",
   "unknown customer id, real vehicle",
   "unknown dealership",
   "unknown service type",
   "unknown vehicle id, real customer",
   "§8.6 then asks that key to distinguish three things it cannot distinguish. Two rows of the"
  ],
  "decision": "Chosen option: B — on 23503 naming appointmentvehicleownedbycustomer, and only then, run one"
 }
}
