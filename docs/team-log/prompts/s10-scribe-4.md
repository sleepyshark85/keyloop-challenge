# Prompt · slice 10 · scribe · invocation 4

Captured at invocation by `.claude/hooks/capture-prompt.mjs`, per METHODOLOGY.md §9.
This file is the prompt **as sent** — written before the agent ran, not reconstructed after.

- Task: Reference the test report in the README
- Sent: 2026-09-08T16:46:14.855Z

---

# Point the README at the generated test report — and fix two things in the same section

Working directory `/home/agentadmin/sources/keyloop-challenge`, **branch `main`**. Pull first. `README.md` is yours under §4.

The human asked: **refer to the test report in the README.**

## What exists to point at

`docs/TEST-REPORT.md`, new, **generated** by `npm run test:report` from the Vitest JSON reporter and Stryker's own `reports/mutation/mutation.json`. It carries a per-suite breakdown, what each suite is *for*, and a per-file mutation table. It is not maintained by hand and says so at the top.

Current figures in it: **829 tests across 59 files, 0 failing** — unit 517, architecture 114, contract 60, property 42, integration 39, acceptance 37, concurrency 15, performance 5. **Mutation 92.00%**, 1392 killed of 1513 across 26 files, **every file at or above §10's 0.75**; lowest `routes/appointments.ts` 76.13 and `server.ts` 77.57, eleven files at 100.00.

## Three changes, all in `## Tests`

**1. Add the pointer.** A row in the command table for `npm run test:report`, and a sentence sending a reader to `docs/TEST-REPORT.md` for the breakdown.

**2. Replace the hand-typed figure.** The table currently says *"Last local run: **829 tests, 59 files, all passing**"*. That number is correct today and will be wrong the moment anything changes — it is the same shape as `A-04-14`, where six hand-copied figures in arc42 §13 were all wrong within two hours of being written, and `O-61`, where a derived file regenerated by hand was stale by default. **Point at the generated report instead of restating its numbers.** If you keep any figure inline, keep at most one and say it is a snapshot.

**3. Fix a stale claim, which I found while reading it.** The section opens *"`npm test` runs the **two** Vitest projects as separate invocations"* — there have been **three** since slice 09 added `perf`, and the table two lines below already says *"all three projects merged"*. The paragraph contradicts itself. While you are there: `perf` exists because a performance budget measured beside the twenty-racer concurrency suite is measuring the runner rather than the service — that was `T-09-3`, raised BLOCKING, and it is worth one clause because it explains why the third project exists at all.

## Constraints

- `README.md` only. **Do not touch** `docs/TEST-REPORT.md` (generated — regenerate it, never edit), `tools/`, `src/`, `tests/`, `docs/slices/`, `docs/arc42/`.
- **Word budget binds** — `npm run docs:budget:check` must stay green and the README is at 1299 of 1500. Replacing restated numbers with a pointer should buy you room rather than cost it.
- **Verify before you write.** Run `npm run test:report` yourself and confirm the file it produces says what you claim it says. Your standing rule is that every claim cites an artifact, and the artifact here is one command away.
- All four doc checks plus `npm run test:tools` green. Commit `docs: `, explicit pathspec, push to `main`.

## Return

The structured report, and anything else in that section you found stale — you have just read it closely, and a second wrong claim in the same paragraph would be worth knowing about.
