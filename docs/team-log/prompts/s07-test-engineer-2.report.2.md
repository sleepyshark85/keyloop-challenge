# Report · slice 07 · test-engineer · invocation 2

Extracted from the agent transcript by `.claude/hooks/log-agent-finish.mjs`.
This is the report **as returned** — it is derived, not retyped, so it cannot drift.

- Task: Slice 07 step 3 red commit
- Returned: 2026-09-06T18:23:25.862Z
- Duration: 2231s

---

**1. Observed `40P01` count and rate.** Six throwaway runs total, in two batches as the fixture was tuned. At `RACE_COUNT=20, TRIAL_COUNT=25` (1000 attempts each): 6, 5, 5 — i.e. 16/3000 ≈ 0.53%. After I raised `TRIAL_COUNT` to 40 (1600 attempts each, to address point 2): 13, 4, 8 — i.e. 25/4800 ≈ 0.52%. Combined: 41 deadlock/500s across 7800 attempts, ≈ 0.5%. That is roughly an order of magnitude below the architect's 117/1000 (11.7%) — but theirs is a different fixture, and mine forces every attempt 1 to fail first (an extra round trip before the two movers' contended attempt 2s are actually in flight together), which plausibly narrows the window the two statements need to align in. I did not assume the 11.7% figure transferred; I measured my own fixture's rate directly and recorded it in the file header rather than inherit the architect's number.

**2. False-pass analysis.** At the observed ≈0.5% per-attempt rate, modelled as independent draws (a simplification, since the two attempts of one pair are correlated with each other, though the pairs are independent): at 1000 attempts (20×25, the AC's literal floor) the chance of observing zero deadlocks by chance is (1−0.005)^1000 ≈ 0.67% — small, but not negligible, and not something I was willing to ship silently. That's why the committed file uses `TRIAL_COUNT=40` rather than 25: at 1600 attempts, (1−0.005)^1600 ≈ 0.034%, about 20× smaller. It is still not zero, and I said so in the header rather than overselling it — this red run's non-zero count is what actually retires the risk for *this* observation, not the arithmetic on its own.

**3. T-07-2's positive witness.** It fired every time, and by construction rather than luck: each mover's own bay is deliberately pre-occupied by a "blocker" appointment at the target interval, so attempt 1 is *guaranteed* to fail and emit a `booking.conflict` line for every one of the 1600 attempts at the shipped config (20×40 pairs × 2 movers). The two witness assertions — `deepestAttempt >= 2` and `conflicts.length >= RACE_COUNT*TRIAL_COUNT*2` — sit earlier in the test body than the 500-check, and in every run I made (roughly a dozen across both trial counts) they never once appeared as the reported failure, which was always the 200/409-vs-500 assertion downstream. So this isn't "some fraction reached attempt 2" — it's all 1600 movers, deterministically, which is what makes the ~0.5% deadlock rate a claim about the union-lock code path specifically rather than about a race that mostly never engages it.

**4. Which criteria passed/failed.** AC-1 (single-mover refusal, `xmin`/`ctid` unchanged) — **passed** at the red commit; slice 06's guarded `UPDATE` was already correct for a lone refused move. AC-2/AC-3 (transient-release barrier, 15 racing bookings against a move destined to fail, 3 repeated runs on the recorded seed 20260907) — **passed**; the single atomic `UPDATE` never gave a racer anything to land in. AC-4 (20×40 mutually-vacating pairs) — **failed**, specifically at "every attempt is answered 200 or 409, never 500," with 4–13 violations per run, every one a `/problems/internal` 500 whose corresponding log line was the structured `reschedule.deadlock` event. So the suite as a whole is red for the intended reason: everything compiles and runs, three of four criteria are already green, and the one criterion ADR-0030 exists to fix is the one that fails.

**5. What step 2 didn't anticipate.** Three things, in order of how much they cost me: (a) `appointment` carries a composite FK to `technician_qualification`, so the two "blocker" technicians couldn't be unqualified outright — I had to give them a third, blocker-only service type, or they'd have needed qualification under A's or B's own service type and reopened the cross-product I was trying to collapse. (b) The candidate space for a move is the *full cross product* of a dealership's bays and its qualified technicians (confirmed by `candidate-retry.test.ts`'s own "16×16=256 pairs"), so a naive 2-bay/2-technician mutual-vacate fixture leaves *two* candidates after attempt 1 fails, not one — an unresolved coin flip neither the design nor I had named until I worked through it. I closed it by giving each mover's own service type exactly one qualified technician, so pruning after the forced attempt-1 failure leaves exactly one candidate: the other mover's bay, deterministically, with no dependence on ADR-0009's seed. (c) The literal string `40P01` never appears anywhere in this service's structured output for the reschedule path — ADR-0029's per-path naming means the only signal is the `reschedule.deadlock` event, with no SQLSTATE text alongside it. A raw substring check (the pattern several existing concurrency files use) would have passed *vacuously* here regardless of build correctness; I only caught this by diffing a raw-output dump against the badAnswers count during validation, and rebuilt that assertion on the structured event instead.
