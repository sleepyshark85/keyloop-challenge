# Prompt · slice 14 · architect · invocation 1

Captured at invocation by `.claude/hooks/capture-prompt.mjs`, per METHODOLOGY.md §9.
This file is the prompt **as sent** — written before the agent ran, not reconstructed after.

- Task: DCR: slice 14 integration test correlation bugs
- Sent: 2026-09-09T14:10:31.813Z

---

DCR raised by the implementer, mid step 4 (GREEN), slice 14 ("The service names itself, and its logs reach the collector"), branch `slice/14-otlp-logs-and-service-identity`, PR #22.

Implementation is complete per `docs/slices/14-design.md` and `docs/slices/14-otlp-logs-and-service-identity.md`: `src/platform/config.ts` (OTEL_SERVICE_NAME, default `keyloop-service-scheduler`), `src/platform/telemetry.ts` (`startTelemetry(config)` now passes `serviceName` and a `BatchLogRecordProcessor({ exporter: new OTLPLogExporter() })`), new `src/platform/otelLogStream.ts` (the pino -> `@opentelemetry/api-logs` bridge, never-throwing, severity-mapped, PII/attribute-scoped), `src/platform/logger.ts` (composes stdout + the bridge via `pino.multistream`), `src/main.ts` (`startTelemetry(config)`), `.dependency-cruiser.js` (`otel-logs-api-only-in-platform`, confined to `src/platform` only, matching the test-engineer's plant exactly — `npm run lint:arch` clean, the plant's 5 tests green). `npm run typecheck` and `npm run build` are clean. Full `tests/unit` (536 tests) and `tests/architecture/layering.test.ts` (32 tests) are green.

Running `tests/integration/telemetry-logs.test.ts` (test-engineer's, owned — I have not edited it and will not): 19 of 21 pass. AC-1 (all three signals), AC-2 (all three signals), AC-3, AC-4, AC-6's first check (no PII), AC-7, AC-8 are all green. Two sub-assertions fail, and I believe both are defects in the test's own correlation logic rather than in the implementation — I have not touched the test to work around either, per CLAUDE.md §5's instruction to raise a DCR instead.

**Finding 1 — AC-6's second check** ("no exported record's attributes add a field the correlated stdout line does not carry"), `tests/integration/telemetry-logs.test.ts:131` (`stdoutLinesFor`) and its use at line 384. It matches a collected OTel record to stdout lines by exact string equality on `trace_id`/`span_id`. For a record with NO active span — `record.traceId === ''`, `record.spanId === ''` (empty string, per `CollectedLogRecord`'s own contract in `tests/support/otelCollector.ts`, "never `undefined`") — no stdout line can ever match, because `src/platform/logger.ts`'s `mixin()` OMITS the `trace_id`/`span_id` keys entirely when no span is active (`if (span === undefined) return {};`), so a line never carries `trace_id: ''`. `stdoutLinesFor` therefore always returns `[]` for an out-of-span record, `allowedKeys` stays empty, and ANY attribute on that record — however legitimate — is reported as "extra". This fires in practice: `src/main.ts:105`'s existing shutdown line (`logger.info({ signal }, 'shutting down')`, pre-dating this slice) runs outside any span, and `NodeSDK#shutdown()`'s flush (already the documented behaviour — design §2's third bullet, "`NodeSDK#shutdown()` already flushes the logger provider") ships it to the collector before the test calls `run.collector.logRecords()` at line ~392, after `service.stop()`. The `signal` attribute is genuinely the same line's own field; the test's correlation just cannot find its own match for the empty-context case.

**Finding 2 — AC-5**, `tests/integration/telemetry-logs.test.ts:459`: `records.find((r) => r.traceId === line['trace_id'] && r.spanId === line['span_id'])` picks the FIRST exported record sharing a (traceId, spanId) pair with a warn/error stdout line. Since one request's whole handler runs inside ONE span (slice 09's own established shape — `src/http/server.ts`'s `serverFactory` span wraps the whole handler; every pino line inside it shares that span's ids), a request that logs "incoming request" (INFO) then an error line then "request completed" (INFO) produces THREE exported records sharing the identical (traceId, spanId). `.find()` returns the first — chronologically the INFO "incoming request" record — not the ERROR record the assertion means to check, so it compares the wrong record's `severityNumber`/`severityText` (9/INFO) against the expected mapping for level 50 (17/ERROR) and fails. This is not implementation-fixable: multiple log lines legitimately sharing one span's ids is the system's existing, already-shipped shape, not something this slice introduced or can narrow without contradicting slice 09.

Both are, I believe, DCR outcome (a) — clarification: the correlation helpers' intent (match a record to *the* line it came from) is right, but the equality-only matching is under-specified for these two ordinary cases (no active span; multiple lines per span), so the test needs a body/severity-aware match rather than trace/span alone. I have not formed a view past that it's the test-engineer's fix, not mine to make.

Please rule per CLAUDE.md §6. If you agree these are test defects, I need to know: does the slice merge with these two sub-assertions failing (documented, re-run once the test-engineer's fix lands), or does step 4 block until the test-engineer revises `tests/integration/telemetry-logs.test.ts`? I have not modified that file and will not without your ruling.

Reference: `docs/slices/14-design.md`, `docs/slices/14-otlp-logs-and-service-identity.md`, `tests/integration/telemetry-logs.test.ts` (test-engineer's), `src/platform/{config,telemetry,logger,otelLogStream}.ts`, `src/main.ts`, `.dependency-cruiser.js` (mine).
