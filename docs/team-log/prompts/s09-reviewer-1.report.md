# Report · slice 09 · reviewer · invocation 1

Extracted from the agent transcript by `.claude/hooks/log-agent-finish.mjs`.
This is the report **as returned** — it is derived, not retyped, so it cannot drift.

- Task: Slice 09 step 5 review
- Returned: 2026-09-07T21:18:26.539Z
- Duration: 2619s

---

Posted: https://github.com/sleepyshark85/keyloop-challenge/pull/20#issuecomment-5575712014

## Report

```json
{
  "role": "reviewer",
  "outcome": "blocked",
  "mutation": {"score": 0.8652, "survivors": 109, "below_threshold": ["src/platform/telemetry.ts 0.1282", "src/application/attemptLoop.ts 0.5985", "src/http/server.ts 0.6944"]},
  "depcruise": "pass",
  "commit_discipline": "pass-with-minor",
  "test_ownership": "pass",
  "dcr": null,
  "findings": [
    {"severity":"BLOCKING","file":"docs/api/openapi.json","line":1,"claim":"AC-9 is not met: the emitted document declares zero `application/problem+json`; all 25 responses, errors included, are `application/json`, while `src/http/problem.ts:76` sends `application/problem+json; charset=utf-8` on the wire.","scenario":"A generated client keys error handling off the declared media type, parses a problem+json body as a success envelope; `collectProblemTypes` (tests/contract/openapi-document.test.ts:75-100) scans the `responses` subtree for `/problems/...` strings and never reads a `content` key, so nothing fails. Second half: `ProblemSchema` unions all nine types onto every 4xx of every operation, so `GET /availability` declares `/problems/vehicle-not-owned`; deleting `422: ProblemSchema` from `src/http/routes/appointments.ts:156` leaves openapi-document.test.ts:224 green."},
    {"severity":"BLOCKING","file":"tests/contract/openapi-document.test.ts","line":1,"claim":"A-06-2 is declared discharged 'over the emitted document — every operation asserted to accept no caller-supplied id … *the reviewer looked* is not executable' (09-observability.md:104-109), and no such assertion exists: zero occurrences of `requestBody`, `parameters`, `appointmentId` or `A-06-2` in the file.","scenario":"Add `appointmentId: Type.Optional(Type.String({pattern: UUID_PATTERN}))` to `BookingBody` in src/http/routes/appointments.ts and run `npm run docs:openapi`. AC-7, AC-8, AC-9 and AC-5b all stay green while ADR-0025's premise — an id unreachable before it exists, which is what keeps the read's `absent` answer permanent — is broken at the API surface."},
    {"severity":"BLOCKING","file":"src/platform/telemetry.ts","line":1,"claim":"Three changed files are below §10's 0.75 per-file threshold — 0.1282, 0.5985 and 0.6944 — on the slice whose stated purpose includes repaying slice 08's identical override.","scenario":"`slice:check` applies 0.75 per changed file since O-64; recorded as-is the slice cannot reach Done. Classification below."},
    {"severity":"MAJOR","file":"src/application/attemptLoop.ts","line":185,"claim":"`booking_conflicts_total{outcome=\"capped\"}` is emitted by code no test in either suite reaches, in either direction.","scenario":"`ConditionalExpression → true` (always 'refused') and the `'capped'` StringLiteral both survive. Unit tests do drive the capped exit (bookAppointment.test.ts:450,478; rescheduleAppointment.test.ts:424) but assert the outcome's `exit` field — the lazy counter is a no-op with no MeterProvider, so they cannot see the label. The telemetry tests can read the counter but DEFAULT_ATTEMPT_CAP is 16 (config.ts:81) and every fixture has 1-2 bays, so they never reach the cap. §8.4 declares `capped` and §11.2 R-4 says a non-zero `capped` is expected in production."},
    {"severity":"MAJOR","file":"src/application/attemptLoop.ts","line":194,"claim":"`booking.attempt`, `bay.id`, `technician.id` (L194-196) and the failed span's ERROR status (L214) are asserted by nothing; L205's `true` mutant also stamps `db.sqlstate=23P01` on a foreign-key failure.","scenario":"AC-2 reads only `db.sqlstate`/`db.constraint`. Blank all three attribute names and the retry waterfall — §1.2 goal 4, and this slice's own DoD screenshot — loses which bay and which attempt each bar was, with every test green."},
    {"severity":"MAJOR","file":"src/platform/logger.ts","line":38,"claim":"AC-6's 'given any request, its logs are correlated to the trace' is certified by one log line out of a whole request.","scenario":"`src/platform/telemetry.ts:122` registers `instrumentations: []` and server.ts starts no span, so no server span exists; the mixin returns `{}` outside an active span. Exactly one log call is inside a span callback — attemptLoop.ts:201. `booking.conflict` (:232), `booking.refused` (:184), the deadlock line (:272), `server.ts:208`'s `request.failed` on every 500, and Fastify's own request/response lines all carry no `trace_id`. telemetry-booking.test.ts:456 asserts `withTrace.length >= 1`."},
    {"severity":"MAJOR","file":"harness/book-read-reschedule-cancel.sh","line":70,"claim":"AC-10's four operations are certified by a digit count, and the `type` half is never asserted or printed.","scenario":"Only the book status is checked; read, reschedule and cancel statuses are never inspected, and harness.test.ts:102-105 asserts `output.match(/\\b200\\b/g).length >= 3`, bound to no operation. Delete the reschedule block (:87-91) and issue the read twice — one 201, three 200s, exit 0, green, nothing rescheduled. Separately, every happy-path response is an `AppointmentView` with no `type` member, so `report()`'s fallback (:57-58) prints the domain `status` labelled `type=`."},
    {"severity":"MAJOR","file":"docs/slices/08-design.md","line":41,"claim":"I-04-5 exits the project undischarged and ADR-0035 exits unruled, with no slice after this one.","scenario":"The slice says I-04-5 is discharged 'by accepting or superseding that proposed decision on measurement, in 08-design.md'; that file is untouched on this branch and still reads '**Proposed, not accepted**'. Commit 9d2daf5 also deleted §11's sentence acknowledging the five retired-proposed decisions, so the free-first ordering decision is now recorded nowhere §11 can see. AC-13 passed at 100ms without the bias — the measurement that would have settled it exists and nobody wrote the ruling. My answer on the gate's question: settle both before merge, because §6(b)'s remedy is a backlog slice and there is none."},
    {"severity":"MAJOR","file":"docs/arc42/08-crosscutting-concepts.md","line":300,"claim":"§8.4 declares two spans and two histograms the merged code never emits, and the design's step-7 edit list removes none of them.","scenario":"`booking.validate` and `appointment.cancel` appear nowhere in src/. `bookingAttempts` and `availabilityQueryDurationSeconds` (telemetry.ts:103,106) are exported and never `.record()`ed — `lazyHistogram` (:74-82) can be deleted wholesale with nothing noticing. An operator wires a panel on `booking_attempts`, whose §8.4 note calls its tail 'ADR-0009's ordering policy working or not' — the exact question §11 R-1 asks — and gets an empty series with no error."},
    {"severity":"MAJOR","file":"src/persistence/db.ts","line":51,"claim":"R-07-12 shipped the drift it names: two literals where the slice promised one derived value.","scenario":"`POOL_MAX = 10` is named here, but tests/concurrency/refused-move-leaves-original.test.ts:161 still declares its own `const POOL_MAX = 10`, and ADR-0013's `outside-in-tests-do-not-import-src` forbids the derivation the obligation asks for. Raise db.ts:51 to 20 and :577 still asserts `movers.length <= 10`, describing a pool that no longer exists."},
    {"severity":"MAJOR","file":"tests/architecture/ambiguity-containment.test.ts","line":369,"claim":"The QS-12 marker — decision 2's only executable form — matches a label spelling, not the counter.","scenario":"It matches `.add\\s*\\([^)]*\\)` whose text contains the bare identifiers `resource` and `outcome`. Its own docblock argues the concept must not be 'one spelling of it', then recognises exactly two. Add `const labels = { resource: 'bay', outcome: 'refused' as const }; bookingConflictsTotal.add(1, labels);` in a second file: two increment sites, every refusal double-counted, and 'incremented from exactly one file' still reports `toHaveLength(1)`."},
    {"severity":"MINOR","file":"harness/book-read-reschedule-cancel.sh","line":87,"claim":"`date -u -d` is GNU coreutils only, and AC-11's 'from a terminal, without the test suite' is not achievable.","scenario":"On macOS the happy-path script aborts at the reschedule step under `set -e`. Both scripts need four pre-seeded UUIDs; there is no seed script in package.json, and README names `harness/` once (:8) with no invocation — so an operator must reach into the test suite, which is what the criterion says to do without."},
    {"severity":"MINOR","file":"src/http/routes/availability.ts","line":136,"claim":"The published contract now explains TypeBox to clients.","scenario":"`docs/api/openapi.json`'s `GET /availability` operation description ends '…TypeBox validates each instant on its own; to <= from is rejected by the route (400), not by this schema, because a schema cannot compare two of its own properties.' It was relocated there to make three literals killable, which the design states as its reason."},
    {"severity":"MINOR","file":"src/http/server.ts","line":138,"claim":"AC-6b left `FST_ERR_CTP_EMPTY_JSON_BODY` in `MALFORMED_BODY_CODES` as dead code; the new parser never raises it.","scenario":"Blanking the literal changes nothing — the mutant survives correctly. Remedy is deletion, not a test."},
    {"severity":"MINOR","file":"docs/team-log/events.jsonl","line":1,"claim":"§7's 'every implementer commit is green' is not observed; §5's half is.","scenario":"CI ran on 4 of the branch's 15 commits and 82139ef failed. d657966 shipped AC-6b's parser change while error-taxonomy.test.ts still pinned the 400 it supersedes, so five commits (d657966..d8ef30e) carried a red contract test until the test-engineer's 5d8a513 — verified by reading the test at d657966. The ownership half is clean: only the two `test(09)` commits touch test-engineer-owned directories."}
  ],
  "message": "Blocked. Three files below §10's per-file floor, AC-9 unmet in the emitted document, and A-06-2 discharged by nothing executable. The attempt-loop extraction itself is faithful and I say so."
}
```

## On O-71 — named, not characterised

**`attemptLoop.ts` 59.85 — the slice-08 argument transfers to about half of it, and I would not accept it whole.**

- **Killed outside `tests/unit/**` by a named, committed, green assertion** (R-12's scope is wrong, the evidence is not missing): `db.sqlstate`/`db.constraint` (L206-207) and `span.end()` (L216-217) die at AC-2, which pins `'23P01'` and `'no_bay_overlap'`; both increment sites and their labels (L185×4, L223[false]/[===], L224) die at AC-3/AC-4, which pin `{resource:'bay',outcome:'absorbed'|'refused'}`; L223[true] dies at AC-5, which asserts zero points on an uncontended booking. I read the collector support: real OTLP over HTTP, real metric points. This is materially unlike slice 08, where no oracle had been emitted yet.
- **Genuinely unasserted behaviour, ~8 mutants**: the `capped` label (nothing reaches it from either side — see the finding), the three span attribute names, the ERROR span status, L205[true]'s mislabelling of a foreign-key failure as `23P01`, and L201[true]'s success line on reschedule's `not-confirmed` path.
- **Unreachable-arm/equivalent, the slice-08 class**: L159×4, L189, L170/172/173, L215, L288. **I nearly filed the bound mutants as behaviour and the arithmetic says otherwise** — maximum conflicts before a refuse is `B+T−2`, so the refusing attempt is `B+T−1`, and both the real bounds and every mutated bound still exceed it. Correcting that here because "name them, do not characterise them" is what caught it.

**`telemetry.ts` 12.82 is three stories.** ~8 mutants (`startTelemetry`'s body, `sdk.start()`, the counter plumbing at L67/L69, `'booking_conflicts_total'`) are killed by the integration test and invisible only through R-12. ~11 are names and descriptions of four §8.4 metrics no AC reads. **~9 are dead code the score is reporting correctly**: `lazyHistogram` (L74-82) and the two histograms it serves are never recorded anywhere in `src/`. "The measurement cannot see it" does not cover that third group — the remedy is deletion or implementation.

**`server.ts` 91.18 → 69.44: 7 of the 22 survivors are killable today, from a unit test that already exists.** L147-152 and L189 are `OPENAPI_INFO`'s title/description/version and `openapi: '3.1.0'` — all in the emitted document, diffed byte for byte by AC-7 in `tests/contract/`, outside Stryker's scope. `tests/unit/http/availability.test.ts:298` already calls `buildOpenApiDocument()` and reads only `paths['/availability']`; reading `doc.info` and `doc.openapi` there kills all seven. That is decision 4's own precedent applied one level up. Of the rest, 11 are the `unreachable()` scaffolding the file declares uncallable, and 1 (L138) is dead code AC-6b left behind.

`availability.ts` at 88.10 hits the projected ceiling exactly — slice 08's overridden 71.43 is genuinely repaid and `D-08-1` closes. That part of the slice did what it promised.
