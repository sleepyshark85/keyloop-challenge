# Slice 14 — as built · [`14-otlp-logs-and-service-identity.md`](14-otlp-logs-and-service-identity.md) · arc42 §7.3, §8.4, §10.2, §11.1 · [ADR-0037](../adr/0037-bridge-pino-to-opentelemetry-in-process.md) · merged

| ref / kind | what |
|---|---|
| decided | Data-model delta none; every new import inside `src/platform`, already a leaf. **The service names itself from its own artifact** — `config.ts` reads `OTEL_SERVICE_NAME` with a default and `main.ts` passes it to `startTelemetry(config)` (ruling **D** of four; **no ADR**, and that line is the record the bar was checked: it closes off no alternative anyone takes twice and adds no second pattern — it restores §7.3's existing one). Rejected on measurement: environment-only leaves a forgetful operator on `unknown_service:node`, and `serviceName` hardcoded on `NodeSDK` makes the variable **inert**, since the option is merged *after* `envDetector` runs. **Logs cross into OTel in-process** — [ADR-0037](../adr/0037-bridge-pino-to-opentelemetry-in-process.md), drafted here at step 1 and written to `docs/adr/` at step 7. **`@opentelemetry/api-logs` is confined, not free**: unlike `@opentelemetry/api` (slice 09 decision 1), §8.4 wants a log line created exactly one way — new forbidden rule `otel-logs-api-only-in-platform`, `src/platform` only and **not** `main.ts`, arriving with its plant (§5.3, QS-10) |
| ruled | **The slice's existence was the human's**, not the architect's: the design declined to rule during phase 6 and put a binary — build the pipeline §8.4 already claimed, or amend §8.4 to retract it. The human ruled **build**, taking the non-binding recommendation; the retraction sentence drafted here is unused. Mid-slice and provisional until the gate: **QS-13 extended rather than a QS-15 added**, goal 4 being one goal a second row would re-home |
| corrected at step 7 | **The design's gloss on AC-2 — *"the criterion option (B) fails"* — was imprecise, and the reviewer named it.** (B) does fail AC-2, but AC-2 is not what separates this design from doing nothing: `NodeSDK`'s `envDetector` reads `OTEL_SERVICE_NAME` unaided, so AC-2's **trace and metric halves pass against unmodified `src/`**, its log half red only because no log record existed at all. **AC-1** — nothing set, and no signal named `unknown_service:node` — is the discriminating criterion, and the one only ruling (D) earns. The criteria themselves did not move; the slice file carries them without the gloss |
| DCR-14-1 | **Ruled (a), clarification** — raised by the implementer mid step 4 against two red assertions in the test-engineer's `telemetry-logs.test.ts`, raised rather than edited (§5). **Measured before ruled**, 19 of 21 green: AC-6's offending record was the `SIGTERM` shutdown line, correlated by string equality on `trace_id`/`span_id` that the mixin **omits** outside a span, against a request-scoped line window it was never in; AC-5's `ERROR` record **was** exported and `.find()` returned the `INFO` record sharing its span, one handler being one span. Not (c) — no AC, `QS-*` or §2 invariant the merged code would fail. Not (b) — a red assertion cannot merge. **Cost: a return to step 3, with no implementation change**, under four constraints so the correction could not be a loosening — chiefly that an uncorrelatable record must **fail**, not be skipped; the test-engineer then *falsified* fail-not-skip with an injected unmatchable record rather than asserting it. **Not a loopback**: §6's governor counts (c), and the red commit `fe574ea` stands untouched |
| step 5 | One MAJOR, one MINOR, both closed and both verified by measurement rather than by a green run. **`pino.multistream` leaves every `StreamEntry` at its own default `info`**, a second gate below the parent logger's own threshold, so `LOG_LEVEL=debug|trace` reached **neither** stream — option (B)'s shape, a §7.3 row that lies, landing in the half of the design that had no ADR. Fixed at `22e0a34` with an explicit per-entry `level`, and the unit-test seam moved **onto** `multistream` too, that bypass being why no seam-based test had seen it. The reviewer ran the four new tests against pre-fix `src/` and all four failed |
| **D-14-1** | the seam's `StreamEntry.level` is asserted by nothing and is invisible to Stryker, whose `ObjectLiteral` mutator replaces the whole entry rather than dropping a key — the reviewer's open MINOR, and the same blind spot as the MAJOR above |
| **D-14-2** | **`OQ-14-1`** — the bridge carries no `exception.*` attributes from `pino`'s `err` serialisation. Named at step 1 as the first thing an operator will ask for; out of scope as designed, deferred to the backlog |
| **D-14-3** | `src/platform/telemetry.ts` at 77.78 %, eight survivors only the Testcontainers suite kills and Stryker's command runner does not run |
| **D-14-4** | `tools/slice/check.mjs` baselines the arc42-declaration criterion on `merge-base(main, HEAD)` rather than the slice's first commit, attributing fourteen pre-slice phase-6 commits here and naming nine files this diff never touches. Booked, not fixed — the orchestrator's tool, and the next slice inherits it |
| assumed | **A-14-1 closed before step 2** — `grafana/otel-lgtm` accepts OTLP/JSON logs on `4318/v1/logs`, probed on the running image: `200 {"partialSuccess":{}}`, and the record came back out of Loki with `trace_id`, `span_id`, `service_name` and `severity_*` promoted to stream labels. Evidence about the image, not about `src/`. **What could not fail, and did not**: AC-8's dead port measures patience rather than isolation on a host that blackholes; AC-5's severity table is transcribed and tied to the bridge by nothing; `api-logs` confinement catches a mistake, not an adversary |
