# Slice 01 — as built · [`01-domain-policy-core.md`](01-domain-policy-core.md) · arc42 §5.2, §8.3, §8.5, §10, §11, §12 · merged

| ref / kind | what |
|---|---|
| decided | **No domain module may import another** — the human ruled AC-6 literal on 2026-09-05, its cost the four debt rows below. **Opening hours reach the pure core as a 7-slot tuple** indexed by `DayOfWeek`, `null` for closed, so absence is unrepresentable; `0 = Sunday`. **A verdict union, not a boolean** — fail-closed must differ from an ordinary refusal, and a boolean lets the right-refusal-wrong-reason mutant survive. **The time parser accepts one exact `24:00:00`** beside `00:00:00`–`23:59:59`, everything else `malformed-hours`. **`durationMillis` is a function, not an exported constant**, or AC-5's scan must reason about call sites |
| test seam | Why the alternatives fail (§8.5 states the seam): widening the ruleset does not work — at the red commit `src/domain/*.ts` does not exist, so a literal specifier fails `tsc` with `TS2307` while a computed `file://` URL into `dist/` exits 0. `tests/unit/` was refused as the implementer's, deferral as writing a property after reading the implementation |
| marker | **`duration-arithmetic` is a concept, not a spelling** — any minutes/seconds ↔ milliseconds conversion under `src/` outside `duration.ts`, where both earlier drafts said the literal `60_000`; six forms implemented. **Four mechanisms make a green scan mean anything**: a corpus guard, a planted control per marker in an unanticipated spelling, a conforming control reporting zero, and a positive *exactly one file matches*, *at most one* being vacuous over an empty `src/domain` |
| ruled | **T-01-2 (c)**, test-first named, **loopback 1 of 2** — a two-project run aborts in setup writing zero tests, so red-proof reports *no suite failed*; the separate-runner remedy was human-ruled tooling prep. **T-01-1 conceded** — convert *before* the call. **Step 4 ×4 conceded**: the `60000` substring match; the parser's range narrowed on measurement; the computed-import hole; *greater than zero* coverage floors **measured to pass ≈29 %** under a broken generator, now computed minima |
| **D-01-1** | composition order left the domain, expressed by a use case rather than the types |
| **D-01-2** | unit confusion across domain boundaries is review-caught, not compiler-caught; both handoffs take a bare `number`, cashed in at step 5 |
| **D-01-3** | one extra branch and verdict variant, `malformed-interval` |
| **D-01-4** | the three-file split is weaker than it looks; of its two justifications only containment survives |
| **OQ-01-1** | with a cleanup buffer, must the **occupancy** interval fall inside opening hours, or only the appointment interval? Deferred, not answered |
| findings | All four debt rows are in §11 and **none argues for revisiting AC-6**. **OQ-01-2 / F-01-2** — AC-5 confines *use of a dealership's `time_zone`* to `openingHours.ts`, yet `referenceRepository` must `SELECT` it; proposed reading, a column name is transport and a zone reaching wall-clock reasoning is use. AC-5 is the human's. **F-01-1** — arc42 §10's DST scenario carried the transposition already ruled a defect in AC-2; corrected at step 1, marked propagated rather than decided |
| assumed | ~~**DA-1**~~ discharged **against** by the human; ~~**DA-2**~~ PostgreSQL's `time` admits `24:00:00`, by measurement; ~~**DA-3**~~ Vitest honours a computed `file://` import of `dist/`, measured with a control; **DA-4** `pg` returns `time` as a string |
