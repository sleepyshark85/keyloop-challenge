# Slice 07 — as built · [`07-reschedule-under-contention.md`](07-reschedule-under-contention.md) · arc42 §5.2, §6.1, §6.3, §8.6, §10, §11 · merged

With [**ADR-0030**](../adr/0030-a-move-locks-the-pair-it-leaves-as-well-as-the-pair-it-takes.md),
`accepted`. Taken as potentially proof-only, and it was not: the step-1 race `F-02-9` asked for came
back negative — **11.7 % of contended move attempts returned `40P01`, and so `500`**, every lock
taken and none skipped.

| ref / kind | what |
|---|---|
| stale leave pair | **The pair a move leaves is read inside the attempt's transaction, under the row's own lock** — ADR-0030's missing half, and a **declared loopback**. Computed once before the loop, the value names a pair the row has left, and the write vacates the row's **actual** pair holding no lock on it. **The cycle needs two stale movers, one per edge.** Lock order becomes **row lock, advisory locks, write**, still acyclic. Carrying the expected pair in the `UPDATE`'s `WHERE` was refused — a zero-row result would mean *not confirmed* **or** *moved under us*, the ambiguity ADR-0025 prevents. Booking it as rare was refused: 11.7 % was rare until measured. Cost: three statements per attempt |
| witnesses | **Three independent witnesses for *never opens a window***, a before/after read being satisfiable by an implementation that released the slot and put it back: the racing bookings (AC-2, only a *committed* release being observable); `xmin` and `ctid` on the refused row (AC-1, *not written*); slice 06's statement-count trigger. **Two test files, no third**, §10 having no headroom |
| interfaces | **`lockResources` gains a required `leave` argument** beside the new `lockAppointmentRow`, so omitting it is `TS2554`; keys are deduplicated and acquired in one statement ordered by class then hash. No data-model delta |
| fixtures | **The barrier harness is reusable; its fixture is not** — AC-2's move-against-bookings race is one-directional and cannot cycle, measured **660 / 660 `23P01`, zero `40P01`**; racing *moves* needs a mutual-vacate fixture with both movers past attempt 1. **AC-5's `pg_locks` join against `hashtext` happens inside one SQL statement**: pulling the object id out compares an unsigned `oid` against a signed `int4`, false on a negative hash |
| criteria | AC-1 gained `xmin` and `ctid`, AC-2 a seed clause, **AC-4** added at step 1 and bounded by the pool at step 5, **AC-5** as the stale-read control. **Mutant controls:** AC-4's reverts `lockResources` to the target pair — **56 / 3000 ≈ 1.87 %** (95 % CI 1.38–2.35) against **41 / 7800 ≈ 0.5 %** (0.27–0.80) unbounded, disjoint intervals, with a **0 / 1000** positive control; AC-5's restores the pre-loop read, run by the reviewer against a reverted build |
| ruled | Step 5, changes-requested with no DCR. **`R-07-1` upheld against the design rather than the build** — this file specified the stale read in the words the implementer built. **`R-07-2`** deadlock freedom rests on **two** mechanisms, collapsed into one by the docblock. **`R-07-4`** remedy re-aimed to a bound stopping the pool queueing a pair's two movers apart. **`R-07-7`** upheld, in scope by `A-05-6`'s own words. **`R-07-10` rejected** — governance tooling is out-of-band, and the Definition of Done is the gate's. **AC-5 DCR (a)** |
| inherited | `O-41` discharged before Ready. `A-06-3` landed as AC-4, its premises re-measured — *cheaper* **false**, *stronger* **true**. `F-02-9` **discharged**, *take both locks* being necessary and not sufficient. `A-05-6` **discharged**, both classifier mutants surviving as ADR-0016's equivalent-mutant residue over 254 adversarial inputs. **Third occurrence of one shape** for the retro (`A-07-3`): slice 06's discharge ruling, ADR-0030's symmetry claim and `R-07-1` each state something true *within* one transaction as though true *across* them |
| **D-07-1** | booked at step 7, deferred to **slice 09**, with `R-07-12` riding in the same §11 row |
| **OQ-07-1** | resolved — an unreproducible budget failure at 893 / 800 was the orchestrator editing two slice files between runs; named because a shared file edited while an agent reads it produces an observation that is correct and unrepeatable |
| risk | The lock statement relies on evaluation order following the subquery's `ORDER BY`; a violation now costs an advisory cycle, which AC-4 catches — measured **0 / 1000** |
