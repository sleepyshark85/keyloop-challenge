# Slice 02 — as built · [`02-book-and-read-an-appointment.md`](02-book-and-read-an-appointment.md) · arc42 §5.2, §6.1, §8.5, §8.6, §10.2 · merged

The slice where the double-booking invariant became a running program; nineteen acceptance criteria.
| ref / kind | what |
|---|---|
| decided | Ten modules over four layers, no data-model delta, use cases returning discriminated unions rather than exceptions (§5.2); a capacity refusal constructible only from a value PostgreSQL produced (ADR-0016); two class-scoped advisory locks per attempt, `40P01` a `500` never retried, one transaction per attempt and none around the loop (ADR-0018, §6.1); `candidateRepository` reading **reference data only**, so check-then-act has no subject; zero service bays ⇒ `500`, no qualified technician ⇒ `422 unknown-reference` (§8.6); the problem `type` a union of taxonomy literals behind a compile-time constructor, the `500` alone with no response schema (§8.5); QS-12's marker split **by concept** into `wall-clock-reasoning` and `zone-transport` (§10.2); the asserted constraint name observable through one `booking.conflict` line per `23P01`; `deriveInterval.ts` carrying the composition order the literal AC-6 ruling took out of the types — **D-01-1** |
| ownership | An unknown vehicle, an unknown customer and a vehicle owned by a *different* customer **all report `23503` on `appointment_vehicle_owned_by_customer`**, so `err.constraint` suffices for `23P01` and never for `23503`. **Validating ownership before the insert was refused** — it renders that `23503` arm unreachable; singleton foreign keys were refused as at slice 00. `OwnershipVerdict` has three members and **none is `ok`** |
| measured | **F-02-1** — which constraint PostgreSQL names under a double violation is **index creation order**; the same pair created in reverse reports the technician constraint. Routed, not landed. An insert violating both the ownership key and an exclusion raises **`23P01`, not `23503`**, so those fixtures must be uncontended. `pg` mappings (`postgres:16-alpine`, `pg` 8.23): `time` arrives as a **string** and `'24:00:00'` round-trips, so AC-19 is reachable with real reference data; `timestamptz` arrives as a `Date` |
| ruled | **Loopback ledger 1 of 2.** **Human** — E-02-1 ADR-0004's retry loop is in scope, without it the refusal names the *abundant* resource and AC-11 breaks; E-02-2 read QS-12's marker by concept; E-02-3 routed, out of scope. **T-02-9 (c)**, naming AC-3, AC-4, QS-1 and QS-2 — a simultaneous loser is refused `40P01`, which this design called a `500`; **spent the loopback**, first under the 2026-09-06 amendment, ADR-0018 following. **(a) ×10** — T-02-1, T-02-2, T-02-3/I-02-7, T-02-4, T-02-8 → F-02-7, I-02-9, I-02-8/T-02-7, I-02-6 (blocking, hence the log line), I-02-5, I-02-3. **Step 4 AGREE ×4**; **R-02-2, R-02-3 (b)**, absorbed by slice 05 |
| **DA-02-1** | the appointment id is minted by the application, not `gen_random_uuid()`; a retried attempt reuses it |
| **DA-02-2** | `AppointmentView` renders instants as ISO-8601 UTC; local time would put zone reasoning in `src/http` |
| **OQ-02-1** | should the edge reject a calendar-invalid but pattern-valid date such as `2026-02-30`? Recommendation **no** |
| **F-02-7** | no seed in slice 02 — ordering is deterministic, so *record the seed* is met more strongly |
| **F-02-8** | `hashtext` is undocumented and internal; any deterministic `int4` per id serves |
| findings | **F-02-6** AC-5 has no runtime leg, a `pg_stat_statements` detector would give one; **F-02-9** every write path to `appointment` must take both advisory locks in one order; **F-02-10** `docs:adr-check` could not see an ADR absent from its baseline, so ADR-0018 landed unpinned; **F-02-11** an instruction stated in prose creates no work item |
| ADRs | [0016](../adr/0016-a-capacity-refusal-requires-a-database-verdict.md) still `proposed`, [0018](../adr/0018-lock-the-bay-and-the-technician-before-each-insert.md) **architect-decided**, [0019](../adr/0019-defer-a-control-only-to-the-slice-that-makes-it-cheaper-or-stronger.md). None for the retry loop, which ADR-0004 had decided |
