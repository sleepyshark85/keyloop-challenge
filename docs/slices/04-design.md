# Slice 04 — as built · [`04-candidate-allocation-and-retry.md`](04-candidate-allocation-and-retry.md) · arc42 §5.2, §6.2, §7.3, §8.4, §11, §13 · merged

| ref / kind | what |
|---|---|
| cap placement | ADR-0004 and ADR-0009 in code; five criteria, QS-3; no data-model delta, no migration; every step-2 and step-4 ruling in the log. **The cap is tested inside the `conflict` arm, never as the loop's bound**, whose header carries the structural `bays.length + technicians.length`, with an unreachable `throw` at the tail. Five trees compiled under `tsc --strict` decided it: the cap as the bound is `TS2322`; a nullable carrier's cheapest discharge is a cast outside the file permitted to make it; a second `BookOutcome` variant splits one §8.6 row for nothing a client can act on |
| `BOOKING_SEED` | **Read by `loadConfig`, unset by default, announced at startup.** Gating it on `NODE_ENV=test` was refused — the tested artifact would differ from the shipped one. Cost: ADR-0009's own named risk, one variable away. **It must not be set in QS-3's cases.** **Narrowed before it froze**: it claimed four things and delivers two — AC-3 and AC-4 are seedless, and a fixed seed forces *a* ordering, never a chosen one |
| `BOOKING_` prefix | **Marks configuration this application invented; an unprefixed name is one something else fixed** (`DATABASE_URL`, `PORT`, `LOG_LEVEL`, `OTEL_EXPORTER_OTLP_ENDPOINT`), so the cap is `BOOKING_ATTEMPT_CAP`. The unprefixed alternative is **unreachable**, not merely costlier: `BOOKING_SEED` is read by test-engineer-owned `tests/support/service.ts`. Of nine backticked `SHOUTING_SNAKE` tokens in arc42 four are not configuration, so the guard that works is set equality between `config.ts`'s `env['…']` keys and §7.3's first column — and `tools/` is not the architect's |
| as built | **The carrier needed two brand casts, not three, and no index assertion** — destructuring head from tail *is* the emptiness test and *builds* the tuple; the shuffle ships in Fisher–Yates **selection** form for the same reason, uniform to **±1.7 %** over 8 bays and 100 000 seeds. **`loadConfig` cannot emit the startup `warn` itself**, the logger being built from its return value; it ships as a pure `configWarnings(config)` emitted by `main.ts` |
| **D-04-1** | ADR-0009 sized the cap below the bound its own reasoning computed — `\|B\| + \|T\| − 1 > 16` at the stated scale, so a capped refusal is expected today and AC-13 cannot pass until it closes. Two remedies, the value the human's; §11.2 |
| **D-04-2** | *capped inside the `23P01` arm* is indistinguishable outside-in from *capped outside it with a cast*, so the placement rule is stated only by `tsc`; a second `continue` leaves the loop unbounded with no `tsc` error, which the tail's `throw` turns loud |
| **D-02-1** | arc42 §11's id for *ADR-0016's argument is weaker after ADR-0018 than before it*, defined here because slice 02's design is closed at its merged budget |
| **A-04-1** | F-02-9's deadlock-freedom argument holds for **three** independent reasons where ADR-0018 gives one — disjoint lock-class key spaces, `lockResources` being one literal `SELECT` over `unnest`, and `domain-is-pure` stopping `candidates.ts` reaching the lock classes |
| **F-04-3** | discharged at step 5 — §11 joined this slice's `arc42:` after step 1, so the step-1 claim of no declared destination was already stale |
| **F-04-1** | `docs:adr-check` reports a new ADR *unpinned* and prescribes hand-editing; third slice running (F-02-10), and `tools/` is not the architect's |
| **OQ-04-1** | the seed is per **request**, and nothing asserted the generator is not degenerate in a deployment. Degeneracy closed at step 3 — two refusals yield two logged seeds, asserted to differ at P(false failure) = 2⁻³². Uniformity not |
| **A-04-10** | §7.3's preamble was false, and it was mine — `OTEL_EXPORTER_OTLP_ENDPOINT` is SDK-auto-configured, not read in `config.ts`. Closed at step 7 |
