# Slice 15 — as built · [`15-seed-fixtures-and-capacity-harness.md`](15-seed-fixtures-and-capacity-harness.md) · arc42 §3.1, §11.1 · [ADR-0038](../adr/0038-the-harness-fixture-carries-data-never-schema.md) · merged

| ref / kind | what |
|---|---|
| decided | **No `src/` change, no data-model delta, no migration** — the whole diff is `harness/**`, one new acceptance file and docs. **The fixture carries data, never schema** ([ADR-0038](../adr/0038-the-harness-fixture-carries-data-never-schema.md), drafted at step 1 and written to `docs/adr/` here): it names entities by key and cannot express a column, so `seed.mjs` stays a second independent transcription of §8.1's shape in hand-written SQL. No uuids, no VINs, no instants, no export names in the file — each preserving a measured property of what it replaced |
| ruled — the trap | Raising the default capacity would have falsified `double-booking.sh`'s *exactly one* `201`, Scenario 2's transcript and slice 10's AC-4/AC-5/AC-6. Ruled **(C) of three**: two subtrees, two scripts, and the scarce subtree carries `exportPrefix: ""`, so slice 10's five exported names are reproduced **by construction**. Rejected: **(A)** raise capacity — amends a merged contract to buy a demo that does not need it; **(B)** parameterise `double-booking.sh` — one script, two claims, and its filename names a hazard. Two dealerships, not one, because bays and technicians are `dealership_id`-scoped. **No ADR** for this: it reverses by deleting a file |
| ruled — scope | *"Enough data for the important cases"* enumerated as nine cases rather than asserted, three of them new: `min(N,M)`, `422 vehicle-not-owned` (a second customer+vehicle, retiring WALKTHROUGH §7's *"seed a second dealership"* by hand), and `422 unknown-reference` for a service type nobody there is qualified for. DST, ADR-0009's cap-exceeded case and QS-14's shape excluded and named. **QS-3 is demonstrated, not extended** — no new quality scenario, no §10 edit |
| corrected at step 2 | **A-15-1's step-1 rationale was wrong and was corrected rather than dropped.** It claimed AC-5's distinctness would notice if the racers serialised. It would not: distinctness among confirmed *overlapping* appointments is **implied by the exclusion constraints**, in any interleaving. The general fact, which also covers ADR-0004's rejected global mutex: **this demo's contention is over persisted rows, not instants** — a sequential run collides with committed rows, retries and prunes identically — so **no response-only assertion discriminates how the answers came about**. A-15-1 is closed on the captured `double-booking.sh` transcript, where racer 4 wins |
| corrected at step 2 | The implementer's coverage note (*AC-3 covers 5 of ~10 validator rules*) was **partly rejected on measurement**: three of the remainder are refused loudly by reference-table CHECK constraints (§11.1 `R-11`), and `purpose` and the ≥1-per-collection minimums fail at AC-1/AC-2's own exports. **AC-3 grew by one case, not five** — collection-key uniqueness, the only silent one — under a stated test: *a rule earns a case when its breach is silent*. Its remedy accepted separately from its finding: **validate-first is not atomicity**, so the insert sequence runs in a transaction; the mechanism was ruled the implementer's discretion, ADR-0038 governing whether SQL is *generated*, not how hand-written statements execute |
| **DCR-15-1** | **Ruled (a), clarification.** The implementer raised, without editing, that AC-3's *"inserts no row"* was implemented as an unscoped `select count(*) from dealership` — racing the `db` project's shared, un-truncated container, whose file parallelism is deliberate (`T-09-3` refused it). Measured 16/16 alone, 1/6 beside `harness.test.ts`, 5/6 across `db`; the implementer's 22-fixture serial control showed the seeder correct. **Not (c)** — the merged seeder satisfies AC-3, so no AC, `QS-*` or §2 invariant was nameable, and §6 makes (b)-or-(a) the outcome then. **Cost: a return to step 3, no implementation change, and not a loopback** (§6's governor counts (c); the count stayed 0). Fixed by the test-engineer under four constraints, of which **C2** — a positive control behind every scoped zero — is what kept the fix from being a loosening |
| step 5 | Two findings, and the pair is worth more than either. **The test-engineer caught a vacuity in its own remedy before committing it**: the corrected assertion matched `/fixture\./` on stderr while its temp file was itself `fixture.json`, so it would have matched the scaffolding rather than the validator's message; renamed to `case.json`. **The reviewer then disproved its own first instrument.** Under validation-off both downstream cases fail through the outer catch, which never echoes the path, so the old name still gave 6/6 and the gap was invisible; a second mutant — keep validation, drop the path from the message — gave `case.json` 6/6 red and `fixture.json` **0/6, fully vacuous**. Recorded here rather than in §11 because it is evidence about how the work was checked, not debt the system carries |
| **D-15-1** | ADR-0009's cap of 16 is deliberately not encoded in `harness/`; a fixture above capacity 8 gets an unwarned flaky demo |
| **D-15-2** | `REQUEST_COUNT` above `DB_POOL_MAX` demonstrates queueing rather than contention |
| **D-15-3** | the seed's `ROLLBACK` path is exercised by hand and asserted by nothing. My step-2 vacuity reason for refusing a criterion is **withdrawn** — the implementer met the SQLSTATE condition I set, with three fixtures that failed on the real code and rolled back to zero rows — but the refusal stands on **§2.4**: minted after the transaction was built, it would have been green on arrival. Its *constraints-fire* half is `R-11`, older than this slice, and step 7 recorded the measurement there |
| **D-15-4** | an architect's citation can **launder a `src/` fact** into a role forbidden to read it — AC-3 cited a migration by line, one of `T-15-1`'s two disclosure routes. `R-11` states the same constraints where every role may read them; AC-3 now cites `R-11`. Standing rule, booked in §11.1 |
| **D-15-5** | **the one slice whose deliverable is a runnable demonstration is the one no human ran.** Gate E delegated the merge without an exploratory pass, so `spurious-refusal.sh`'s only end-to-end runs are the implementer's (10 racers → 3 confirmed on 3 distinct bays and 3 distinct technicians, live service) and the reviewer's replications. Weaker than slice 14, where the human observed the join in Grafana |
| assumed / could not fail | **A-15-1** closed above. **No mutation evidence exists for this slice** — nothing under `src/` changed and Stryker mutates neither `harness/**` nor tests — so the four negative controls (AC-3, AC-6, AC-7, AC-8) are the substitute, and step 5's two findings are the reason to believe them. **`T-15-1`**: the test-engineer disclosed reading `src/` unprompted; the reviewer enumerated every SQL identifier in the committed file, traced each to a legitimate source and measured contamination **nil**; the human recorded the breach with no consequence, half its cause being `D-15-4` |
