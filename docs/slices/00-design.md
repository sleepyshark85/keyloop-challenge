# Slice 00 — as built · [`00-schema-and-exclusion-constraints.md`](00-schema-and-exclusion-constraints.md) · arc42 §8.1, §8.2, §7.2, §11 · merged

| kind | what |
|---|---|
| decided | The phase-4 pilot, proven by SQL with no application code in existence. **No `IF NOT EXISTS`**, `CREATE EXTENSION` excepted — idempotence is the runner's job. **`-- Up Migration` is the first line of every file**, everything above it never reaching PostgreSQL; all three carry `-- Down Migration`. **Errors are read off `pg`'s `DatabaseError`**: `code`, `constraint`, `table`, never `message`, `detail`, `hint`, `severity`. **`beforeAll` may only connect**. **Adjacency proves the range is *not closed*, and no more** |
| fixtures | **A test-engineer-owned loader**, derived ids and VINs, no `ON CONFLICT DO NOTHING`. A migration, a `src/` loader and a checked-in `.sql` were all refused: each puts the fixtures beyond a boundary the test-engineer may not cross. Cost: partial isolation, a second transcription of the schema. **Architect-ruled, never human-ratified** |
| case 0 | **Asserts the schema *is* arc42 §8.1's** before any other case is evidence — three migration names, `btree_gist`, nine relations, seven non-primary-key `appointment` constraints by **equality** on `pg_get_constraintdef` against a hand-transcribed literal; the filter is an **allowlist** |
| isolation rule | One violable constraint per negative case, named rather than left to the SQLSTATE (`23503` has four producers here), each with a **positive control**, negative case first |
| ruled | Twelve findings, **all clarifications, no loopback**. **T-4** equality on `pg_get_constraintdef`; **T-5** positive controls and the ordering rule; **T-6** AC-3 step 5 kept as coverage, its reason false; **T-7** remedy to a second test-engineer commit, not the implementer; **T-8** set equality on constraint names; **T-9** the human's; **I-8** fallback replaced (M-10); **I-9** ADR-0007 drift; **R00-3** proven to exist, never to fire; **R00-4** controls for AC-1 and AC-2; **R00-5** §8.1, §11.2; **R00-1/2** process, R00-2 producing the human's `N/A` gate state |
| measured | 2026-09-04, `postgres:16`, `pg@8.23.0`, `node-pg-migrate@9.0.0`. **M-1** `CHECK` fires first, `23514` / `appointment_interval_ordered`, never `22000`. **M-2** exclusions pre-empt the FK triggers, both violable reporting `no_bay_overlap`. **M-3** `btree_gist` 1.7, predicate re-rendered `WHERE ((status <> 'cancelled'::appointment_status))`. **M-4, M-5** a malformed `0003` diverges across the two entry points on `--single-transaction`. **M-6, M-7** filename order; a re-run applies `[]`; the downs reverse. **M-8** `code`, `constraint`, `table`, `schema` populated on all three SQLSTATEs. **M-10** the failing migration's name is only in the CLI's last `### MIGRATION <name> (UP) ###` header. **M-11** clean on 16.15 and 17.11; PostgreSQL 18 emits `NOT NULL` constraint rows |
| assumed | **A-1** Vitest's shape for a `beforeAll` failure; **A-2** which exclusion is reported when both are violable; **A-3** two literal VINs would collide; **A-5** the planner chooses the partial GiST indexes. ~~**A-4, A-6, A-7**~~ closed, two of them wrong |
| debt | Five in §11. Only here: **restore the migration logger in `tests/setup/postgres.ts`** (M-10 is a working fallback), and **the path guard does not enforce the shared `tests/integration/` boundary** (the enforceable form needs the hook to read contents) |
| ambiguity | AC-7 names no constraint where AC-5 and AC-6 do — fixed at `appointment_bay_in_dealership`. AC-9's *deterministically* is the derived ids, its *every reference table* the eight non-appointment relations. AC-4's *status is set to `cancelled`* is a direct `UPDATE` |
