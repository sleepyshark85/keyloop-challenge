# Slice 08 — as built · [`08-availability-query.md`](08-availability-query.md) · arc42 §5.2, §6.5, §8.6, §10.2, §11 · merged

| ref / kind | what |
|---|---|
| decided | `GET /availability`, advisory by contract and in agreement with the constraint under quiescence; no data-model delta. **Availability is two reads composed in the use case** (§6.5), the GiST indexes serving the range predicate. F-08-1's joined query would widen the `appointment-table-access` marker to two files, and the marker is per **file**, so §5.2's *nowhere else that could read* stops resting on impossibility; a separate `availabilityRepository.ts` widens the list for no gain. **The marker's file list is unchanged by this slice, and that is the assertion**, discharging slice 05's AC-1 re-derivation. Cost: two round trips for one join |
| advisory | **§2.1 is silent about the endpoint, and that is the finding** — its subject is the `INSERT`, and a `GET` performs no act. *Advisory* stays true structurally: with **no representation of a hold**, nothing turns a free answer into a reservation. Nothing counts a wrong answer (§11) |
| ordering | **The advisory read orders candidates; it never removes them — and it is not this slice.** A read that *removes* candidates can empty the list, routing to `500` or `422` and never `409`, both wrong for a merely full dealership. Filtering with a fallback was refused; raising the cap is the human's. **Proposed, not accepted** |
| `I-04-5` | **Declined, not deferred** (ruled at slice 09 step 5) — free-first was warranted only as *how* AC-13 is met, and AC-13 has since been **measured** met without it, p95 under 100 ms and exactly one `INSERT`. Reopen only on a measurement |
| QS-8 | AC-1's six mechanics, each closing one way the property passes while wrong: a rolled-back `SAVEPOINT` per probe; the verdict `23P01` exactly; witnessed quiescence; the two directions counted apart; boundary-biased generation; a cancelled witness by construction. **No isolation level substitutes for mechanic 3** |
| arc42 defects | **F-08-1** — §6.5 had specified `candidateRepository.freeResources` since phase 2 and no such function existed, while an architecture control planted that exact call as a violation. **F-08-2** — QS-8 was false as §10.2 wrote it. Both corrected in place |
| ruled | Three rounds, **no loopback**: no AC, `QS-*` or §2 clause was nameable. **AC-1 amended** — candidate universe, probe interval exactly `[from, to)`, mechanics required, a failed quiescence witness failing *invalid*. **AC-5 split**, AC-5b → 09. **AC-6 amended** to `to <= from` (**F-08-3**), having guarded the window the opposite way from the database. **AC-7** minted at step 1, **withdrawn at step 2**. **`R-08-1` (b), built here** — `status` was a dimension the generator never varied. **`R-08-3`** the mutation config includes `tests/unit/**` only, so the score is a floor on unit rigour. **`R-08-4`** upheld, no action — 151 lines before their unit tests, so §7's *green* held and its *together* did not. **`T-08-7` (a), my specification at fault** — 8 of 35 trials survived at `NUM_RUNS = 30`, mechanic 6 becoming a **construction** at 20 consecutive kills. **`I-08-6` (d)** 6 → 5. **`A-08-3` (b) with a deviation**. Six further **(a)**; `O-59` the human's |
| `R-08-5` | **Upheld whole, offered remedy rejected, (d)** — §10's per-file threshold is not met on `routes/availability.ts`; removing all 24 survivors from the denominator inverts `I-06-5`, and widening the Stryker `include` kills none of the twelve that matter. **(c)** unavailable, nothing nameable failing; **(b)** available and wrong, a missed threshold not being a deferred improvement; so **(d)**, the trade-off being the gate's, and `gate: light` was revoked |
| `O-62` | **(d)**, round 3 — **the arithmetic error was mine: 71.43 %, not the 76.19 I published**, `disable next-line` counting from the end of the comment block. **A third directive is refused on my own record** |
| **D-08-1**, **D-08-2** | `routes/availability.ts` merged at **30 of 42 = 71.43 %** against §10's 0.75. **D-08-1 closed at slice 09: 88.10 %, the projection exactly** |
| **D-08-3** | `A-08-3`'s backlog item, hosted by no live slice |
| assumed | **A-08-1** pair-decomposition holds while nothing couples bay and technician. **A-08-2** mechanic 3 makes *quiescent* a measurement, assuming an idle container. **OQ-08-1** ids, not names or capacities. **A-06-4, for the gate** — §11 R-5's missing runtime signal, a `23P01` on **attempt 1** against a pair the read called free, survives |
