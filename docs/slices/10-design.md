# Slice 10 — as built · [`10-openapi-and-curl-harness.md`](10-openapi-and-curl-harness.md) · arc42 §3.1, §8.5, §8.6, §10.2, §11.1 · merged

| ref / kind | what |
|---|---|
| decided | QS-11; data-model delta none. The through-line: **all three subjects were asserted by tests that could not fail**, so every ruling was judged on whether it produced an assertion with a reachable red. **The matrix is a column, not a second table** — §8.6 already owns status, `type`, when and decided-by; shipped on its nine rows, `/health` excluded **by name** with an asserted-empty problem set rather than by silent omission. **Equality on (status, type) pairs, both directions** — a superset passes on the defect being removed, and type sets alone miss `malformed-request` drifting 400 → 422. **No ADR**, checked against the human's 2026-09-07 bar and recorded as checked: per-cell narrowing refines ADR-0024's closed set rather than closing an alternative |
| measured | **Measure, do not choose in advance** (`D-09-3`'s precedent), fixing the property and not the mechanism. **M2 — a one-member `Type.Union` collapses to a literal and silently substitutes**, §8.5's documented defect reintroduced inside the fix for it, reached by **eight** cells; `Type.String({ enum })` measured *worse*, so the remedy is a hand-built `Type.Unsafe` carrying a one-member `anyOf`. **M1** — the `content` form keeps the serialiser and survives charset but fails differently from the bare form (`I-10-2`). §8.5 took both |
| inherited | Both closed. **`R-09-13`** contract prose to the operation-level constant, TypeBox rationale to the docblock, the duplicate `description` dropped as measured discarded. **`R-09-12`** the acceptance test drives both scripts from **only** what the seed printed, never the fixture's ids, with a GNU-only-utility denylist over `harness/*.sh` |
| ruled | Mid-slice authority, provisional until the gate. **AC-7's boundary corrected** to *`to` must be strictly later than `from`*; as written it published a rule the code does not implement. **AC-3b added** — AC-3's `requestBody` and `(name, in)` equality discharges `A-06-2`'s **contract** half but not *`deps.newId()` is the only mint*; a `tests/architecture/` marker asserts the uuid-minting file set equals `{src/main.ts}`, a denylist and not a proof. **`REQUEST_COUNT >= 2` and the negative control move together**, a bare guard otherwise colliding with step 1's one-request control; the control now fires **two** racers, zero `201`s, two `409`s, non-zero exit |
| step 5 | Eight findings, eight upheld, **loopbacks 0 of 2 and unchanged**. `R-10-1` and `R-10-3` block the merge as **conformance defects**, not DCR outcomes — the design was right, stated and not built — and **neither is softened by that**. **`R-10-1` blocking** — the README run section plus an assertion; my *gate-verified rather than mechanical* **withdrawn**, remedy **strengthened** to set equality between `package.json`'s `harness:*` keys and `README.md`. **`R-10-2` (a)**, the ambiguous wording mine — the behavioural property at the **runtime schema**, and at **document** level, where `@fastify/swagger` rewrites `const` to `enum` and `fast-json-stringify` passes it through unvalidated, **shape**: a one-member `anyOf`. A mechanism assertion, which step 1 declined by name; **I reverse that for the document level alone**. **`R-10-3` blocking** — `/health` by name; `EXPECTED_PAIRS` had five keys, so **AC-1 fails**, and the remedy closes the **class**, its key set equalling `doc.paths`. **`R-10-4`** derive single-type cells from `EXPECTED_PAIRS`. **`R-10-5`** remedy amended. **`R-10-6`** upheld as measured, no code (`F-06-2`). **`R-10-7`** compute the seed's date. **`R-10-8`** out of scope — `D-09-4` and `D-07-1`'s saturated-pool half |
| **D-10-1** | §8.6's operations column and the contract test's matrix are two transcriptions tied by nothing (§11.1); parsing §8.6 from a test was declined, it making arc42 a machine-readable input. No destination slice, there being none |
| assumed | **A-10-1** the four dropped `(operation, status)` cells are unreachable, read from the routes' exhaustive switches; if any is reachable, QS-11's sweep reports it on the next commit. **OQ-10-1, closed by measurement** — `SwaggerParser` accepts an unreferenced `components.responses` entry, and narrowing left a **second** type homeless: `internal` (`I-10-4`) |
