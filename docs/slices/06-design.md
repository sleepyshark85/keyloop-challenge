# Slice 06 — as built · [`06-reschedule-atomic-move.md`](06-reschedule-atomic-move.md) · arc42 §5.2, §6.3, §6.6, §8.2, §8.6, §10, §11 · merged

With [**ADR-0025**](../adr/0025-existence-is-the-reads-legality-is-the-statements.md). No data-model
delta; `PROBLEM_TYPES` went 7 → 9. Re-ruled at step 1: **racing moves deferred to slice 07**
(`A-06-3`), **`src/domain/appointment.ts` retired**.

| ref / kind | what |
|---|---|
| lock | **The lock is a value the write takes, and it carries the keys it took** — `lockResources` returns a branded `ResourceLock` holding `bayId` and `technicianId`, so *forgot the lock* is a compile error and *correctly exempt* is readable off `cancelAppointmentById`'s signature. A docblock, a rule and a QS-12 marker were refused: markers are file-granular and the two functions share a file |
| attempt 1 | **A move attempts the pair it already holds before it shuffles**, bound one higher. Shuffling first was refused because **AC-1 could not then fail** and `T-06-3`'s discarded-candidate case becomes unconstructible; distance-ordering is allocation policy, which §3.3 excludes |
| events | **A deadlock event names the write path; a conflict event does not** — `DEADLOCK_EVENT` is `'reschedule.deadlock'` or `'booking.deadlock'`, the other three names shared because a `23P01` is the same fact whichever statement provoked it, where a `40P01` is no taxonomy row and its whole content is *which path*. One name plus a `path` field is probably better, refused as a third design at step 7 over merged code; cost, two counters for one rate |
| measured | **The exclusion mechanism, off `check_exclusion_constraint`** (`A-06-1`): the check skips the new entry by `ctid`, so the superseded version is *deleted by me* — a property of the **enforcement mechanism**, where a `BEFORE UPDATE` trigger is correct only if someone wrote `AND o.id <> NEW.id`. **`problem.ts` is immune to mutation testing, not cushioned** (`I-06-1`): **12 mutants with and without the two new rows, 9 killed, 75.00 either way**. **The exhaustiveness disables over-applied elevenfold** (`R-05-9`): **93 mutants suppressed where 8 were ruled**. **After remediation every changed file clears §10's 0.75** (`I-06-5`), reschedule rising **70.00 → 92.50** with exactly the 9 survivors the reviewer predicted; eight unkillable mutants remain and the suppression was **deliberately not widened** |
| ruled | **AC-5 amended** (ADR-0025) — a move cannot be constructed without reading its own row, so the amendment is *stricter*, forbidding the follow-up read §6.3 mandated; `T-06-1`, the falsifier needs a statement-level companion. **AC-1 corrected twice**, both omissions the architect's (`T-06-6`, `I-06-2`). **`T-06-2`** two controls, not one, on the constraint **name**. **`R-06-1` (a)** AC-2's window opens after the fixture's arrange; `pg_stat_user_tables` and `xmin` rejected as instruments. **`OQ-06-1`** out of scope — a move to the same instant is a request, not a replay |
| **D-06-1** | the move guards on an allowlist where the constraints denylist (ADR-0025) |
| **D-06-2** | a `ResourceLock` cannot be forgotten and *can* be forged, a brand erasing at runtime |
| **D-06-3** | AC-2's audit instrument can fail falsely, never pass falsely (`T-06-7`) |
| **D-06-4** | a `rescheduleAppointment.ts` docblock carries the event-naming rule above |
| **F-06-1** | two attempt loops, one design; extraction to slice 09 |
| **F-06-2** | `problem.ts` has no margin, its taxonomy invisible to the mutation score |
| still open | **`A-06-2`** nothing asserts `deps.newId()` is the only id-minting site, which ADR-0025 rests on; deferred to slice 09. **`T-06-5`** the type does not prove the write runs in the lock's transaction; the test-engineer's contribution was a **negative result**, no black-box test observing it. Ruled **(b)** by the architect against itself — nothing nameable, and ruling (c) on an improvement the adjudicator invented against a day-old decision is the failure §6 names. **`A-06-4`, `A-06-5`, `A-06-6`** to the retro |
