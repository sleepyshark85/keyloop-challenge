# Slice 05 — as built · [`05-cancellation.md`](05-cancellation.md) · arc42 §5.2, §6.1, §6.4, §6.6, §8.6, §10, §11 · merged

ADR-0003 in code, raising [**ADR-0024**](../adr/0024-the-error-taxonomys-residual-is-a-property-not-a-row.md)
and narrowing one sentence of F-02-9. No data-model delta.

| ref / kind | what |
|---|---|
| **D1** | Idempotency is one unconditional `UPDATE` with a `CASE` holding `updated_at` on a replay — no guard predicate, no pre-read, so check-then-act is never invoked. Measured: the guarded alternative also returns zero rows on a replay; a plain `now()` makes a replay a client-reachable write; the `CASE` changes no column, though `xmin` advances 739 → 740 |
| **D2** | Cancellation takes **no** advisory lock; F-02-9 narrowed to an *iff* — a transaction takes both locks iff a statement in it writes a row version **into** an exclusion constraint's scope. ADR-0018 **not superseded**. Measured: an inserter waits on an uncommitted cancel, then gets `201` (M1); the cancel never waits on an exclusion check (M2, 2 ms); twenty cancels racing twenty inserts on one bay give **zero `40P01`** with no lock taken (M3) |
| D2 refusals | Obeying F-02-9 literally costs a pre-read reintroducing read-before-write, and M2/M3 show it buys nothing. Keys from `RETURNING` are impossible; a third lock class on `appointment.id` was refused, cancels of one row already serialising on it. **The unit is the transaction, not the statement**, and ***into*, not out of** |
| **D3** | AC-2 needs no change to slice 02's read path, zero production lines — **half withdrawn at step 2**, it failing at step 3 like the other three |
| **D4** | `gate: light` revoked — three of four named risks did not happen, and the fourth arrived in a form none named |
| AC-1 | **Its stated ground was wrong twice**, the finished claim being the test-engineer's: slice 00 already pins the *not cancelled* predicate definitionally and behaviourally, `candidateResources` never reads `appointment`, and `freeResources` serves **slice 08's endpoint, which AC-1 never calls**. Slice 08 owes the re-derivation |
| corrections | D3's claims were put to a role **forbidden to read `src/`**, so agreement would have been deference wearing verification's clothes. AC-3's `CASE` became falsifiable only because step 2 insisted — *the response body is identical* is satisfied equally by the `CASE` and the `now()` it rejects; AC-3 is asserted twice, its integration half by `to_jsonb` equality over every column |
| ruled | Standing delegation, provisional until the gate. **OQ-05-1 → AC-5** both content-type-parser codes map to `400 /problems/malformed-request`, **by code**, not by `statusCode < 500`. **Scope** one concurrency file, respecified at step 2 so it can fail for the reason it exists. **R-02-2, R-02-3** build now, not a fourth deferral. **R-05-2 (a)** every deferral sits in its destination file — F-05-1 → 06, AC-1's ground → 08, OQ-05-2 → 09, A-05-6 → 07. **R-05-3 + DCR (d)** → ADR-0024, §8.6 corrected **now**, the handler waiting for slice 06 because registering it breaks AC-4's vacuity guard. **R-05-4** accepted. **Exhaustiveness disables** narrowly at slice 06, the `const unhandled: never` arms only. **Phase 4's simultaneity** asserted there alone, where `1` **is** the claim |
| **F-05-1** | two write functions in one file, one locking and one correctly not, where *correctly exempt* reads identically to *forgot the lock*; the QS-12 route is closed, markers being file-granular |
| **OQ-05-2** | an empty body on a bodyless route answers `400` — deferred to slice 09 after first being routed to a slice tombstoned two days earlier, the R-05-2 failure again |
| **D-05-1** | ADR-0024's second exit ships knowingly, `src/http/server.ts:36` still asserting §8.6's totality *is kept* there |
| **D-05-3** | ADR-0019's deferral criterion, 0-for-2 on its own premises at its first destination |
| assumed | **A-05-1**, amended at step 2 — AC-3 means *no column changes* **and** *the response body is identical*. **A-05-2** — M1–M3 are three readings of one PostgreSQL version; the structural argument carries the *iff* |
