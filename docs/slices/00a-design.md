# Slice 00a — as built · [`00a-walking-skeleton.md`](00a-walking-skeleton.md) · arc42 §5.2, §5.3, §7.1, §7.2, §7.4, §8.5, §11.2 · merged

| kind | what |
|---|---|
| decided | **Nine files**, `src/domain` empty behind a `.gitkeep`. **Composition root by partial application** — `main.ts` holds the only database handle, so nothing above names Kysely. **One container per run**, no reuse, Vitest provide/inject, isolation by data, migrations unconditional. **Two Vitest projects** split on database need, one results file for red-proof. **The service is compiled; no TypeScript loader**, so `npm ci && npm test` satisfies AC-1 literally. **`lint:arch` is a wrapper, not the bare CLI**: repository-local `depcruise`, **no `PATH` fallback** (exit 2), fails per **root**, one cruise. **The CI collector writes nothing it did not compute**, exits 2 rather than guessing. **Red-proof reads arguments, never the environment**; only `tests/unit/` must pass, the other test-engineer directories are the red zone. **The outside-in rule names those seven directories** |
| `GET /health` | **an operational probe, not a sixth operation** — same body on both codes, never `problem+json`, no taxonomy row, the contract stays **five** operations; one-second connect timeout, no connect at boot. Cost: no liveness/readiness split, a second endpoint refused because nothing would consume one. **Architect-ruled, never human-ratified** |
| ruled | **O1 (c)**, AC-4 and QS-10 named — assert cruise coverage. **S-1 (c)** self-raised, the design worked around test-first; the tool suite joins the red commit's CI job. **O-1 (d) → human: AC-6 reads broad**. **(a) ×7** — O2, O3, O-2, F1/F2, F6/F7, J-1…J-3, A-1: compile and own `tests/support/`; the literal tool chain; collector constraints 4–5; fail per **root**; ordering and `engines.node`; whole cruise result, last-`/tests/` fallback, name→key map; the forbidden `PATH` fallback → exit 2. **No loopback consumed.** Human: arc42 gains four sections, `docker-compose.yml` starts PostgreSQL and telemetry only |
| measured | **Inert and clean cruises are indistinguishable at exit 0** — `totalCruised 0 · modules 0`, `issues[0].name = "missing-typescript-transpiler"`; absent and out-of-range compilers are byte-identical there on `dependency-cruiser@18.2.0`, so **no version guard is constructible**. **The declared Node range promised support the tree refuses** → `">=22.22.0 <23 || >=24.0.0 <25"`. **The migration tool on a `.gitkeep`-only directory is a clean no-op**; no zeroth migration. **Five gate constraints on the collector**: lowercase `"pass"`; `FAIL` **iff** the run failed; no ratio strings; **oldest-run-first**; the run's own update time; absent the step, `"not-run"`. **The red was observed**, replayed offline at exit 0 |
| debt | Gate should order CI records by timestamp, not position; the tool suite should discover its test files; the module-graph script renders empty and clean identically; `docker-compose.yml` does not start the service (human-ruled); **standing** — re-check the supported compiler range on any bump, and anchor red-zone and must-pass lists at `^tests/` |
