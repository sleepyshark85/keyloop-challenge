# Slice 09 — as built · [`09-observability.md`](09-observability.md) · arc42 §3.1, §5.2, §5.3, §8.4, §8.5, §8.6, §10.2, §11 · merged

| ref / kind | what |
|---|---|
| decided | Gate D folded two slices in, `O-59` admitted a sixth inherited obligation, and on 2026-09-08 the human split it again; data-model delta none. **The API facade is free; the SDK is confined** — `@opentelemetry/api` importable anywhere, the SDK and its exporters in `src/platform` plus `main.ts`; shipped with `otel-sdk-only-in-platform` **and its plant**, a new forbidden rule arriving with one or not arriving. **`booking_conflicts_total` is incremented from exactly one module**, auto-instrumentation plus two spans being how one conflict is counted three times; **restated at step 5**, the merged code having two `.add` expressions in one file and the rule protecting that one conflict is counted once. **Timing and counting are two measurements**, figures taken with statement spans off and the `INSERT` count with them on. **The document is emitted by a callable function**, not only a script — **measured at 88.10 %**, the projection exactly, closing `D-08-1`. **`D-07-1` is split**: in, the pool ceiling as one value; out, what a saturated pool *answers* — remedy **inverted at step 5**, an outside-in test dictating `DB_POOL_MAX`. **`T-06-5` accepted at step 1 and not built** |
| ruled | Mid-slice authority, provisional until the gate. **AC-5b** added, `R-08-2` having named it at slice 08 unwritten. **`T-09-2`, `T-09-4`** one red set as a **property, not a count**, with a **closed** exclusion met by exactly one criterion; AC-12 is a standing guard. **`T-09-1`, `T-09-3`** the document is `docs/api/openapi.json` from `buildOpenApiDocument()`; `tests/performance/**` runs alone in a `perf` project; AC-10's `type` is an error-response word. **`O-70`** in for diagnosis, out as a test shape — bounding racers to the runner's CPU count is refused, QS-1 fixing *N* = 20. **`I-04-5` declined, not deferred.** **`T-06-5` declined, not deferred.** **Slice split** at step 7 on the human's `A-09-4`: AC-9, AC-10, AC-11 and `A-06-2` to [slice 10](10-openapi-and-curl-harness.md) |
| step 5 | **Ruled (c), design defect — the project's first. Loopback 1 of 2.** Named, as §6 requires: **AC-9** and **QS-11**, the document declaring `application/problem+json` on **zero** of its 25 responses while `problem.ts:76` has sent it since slice 03; and **AC-6**, *given any request* being false when one log call sits inside a span. Both causes are design — §8.6 never mapped a `type` to its operations, and §8.4 declared a server span *(auto)* that decision 1 gave no instrumentation. Fifteen findings, three BLOCKING; four adjudication rounds before it, eight objections, one DISAGREE (`I-09-4`, (b), ADR-0035). **All three BLOCKING findings sat on the contract half** — the evidence Gate D's fold was wrong |
| **D-09-1** | the contract describes a media type it does not send; all 25 responses declare `application/json`. Slice 10 |
| **D-09-2** | `ResourceLock` still does not carry the `Db` it was taken on — accepted at step 1 because `F-06-1`'s extraction made it one signature, declined at step 7 on the test that made it (b) at slice 06 |
| **D-09-3** | `@opentelemetry/instrumentation-http` does not patch under this entry point — measured, it left `http.Server.prototype.emit` alone, `require-in-the-middle` never seeing a native ESM `import` |
| **D-09-4** | `I-09-7` is resolved on evidence, not disproved: three green CI runs against a two-of-three local rate |
| **D-09-5** | ADR-0035 is `proposed` and §6(b) has no terminal case — a project's last slice cannot cut the backlog slice the remedy needs |
| **D-09-6** | QS-14's headroom is the regression baseline, ≈9 ms against a 200 ms ceiling; if `T-09-4` was wrong, only a 22× regression fails the availability half |
| assumed | **A-09-1** `O-70` is runner starvation, untested. **A-09-2** the CI container's class is stable enough for a threshold to mean anything between runs. **OQ-09-1** `D-07-1` has **no log ref**, so the inherited-scope guard cannot see it — the `R-05-2` shape again, the orchestrator's to fix |
